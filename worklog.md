# Edify LMS MVP — Worklog

This is the shared worklog for all agents building the Edify LMS MVP prototype.
Each agent MUST append (not overwrite) its own section using the template below.

---
Task ID: 1
Agent: main (orchestrator)
Task: Foundation — design system, store, mock data, i18n, shared types

Work Log:
- Read the build prompt at `/home/z/my-project/upload/lms-mvp-build-prompt.md`.
- Inspected project scaffold (Next.js 16 + Tailwind 4 + shadcn/ui already installed).
- Designed the LMS brand system in `src/app/globals.css`:
  - Primary: violet/indigo `#5624D0` (Udemy-inspired).
  - Accent: Coursera blue `#0056D2`.
  - Neutrals `#1C1D1F` / `#6A6F73` / `#F7F9FA`, success `#1E7F45`, rating gold `#F5C518`.
  - Dark mode tokens included. RTL font switching (Cairo for Arabic, Inter for Latin).
  - Custom scrollbar, view-enter & slide-enter keyframes, AI typing-dot animation.
- Updated `src/app/layout.tsx` to load Inter + Cairo fonts and set `<html dir>` default ltr.
- Created `src/lib/types.ts` — full domain model (Course, Lesson, Section, Instructor, Review, QAItem, FAQItem, Role, Skill, Quiz, Certificate, CartItem, Route, OnboardingAnswers, CourseProgress, etc.).
- Created `src/lib/i18n.ts` — three dictionaries (English, Arabic-Saudi, Arabic-Egyptian) with `translate`, `formatNumber`, `formatPrice`, `formatDate`, `formatDuration`. Saudi tone uses "يلا نبدأ", Egyptian uses "يلا بينا".
- Created `src/lib/mock-data.ts` — 18 realistic courses across Data / Web Dev / Design / Product / Marketing, 5 instructors, 8 roles, 27 skills, 1 quiz, semantic search map, promo codes.
- Created `src/lib/store.ts` — Zustand store covering: i18n (locale+dialect), SPA routing with history stack, auth (signUp/logIn/loginAsDemo), onboarding (step + answers + completion), cart, wishlist, enrollments, per-lesson progress with auto-resume target, notes, extra Q&A, certificates, quiz attempt state, toasts, and a confetti token.
- Created `src/hooks/use-i18n.ts` — `useI18n()` hook returning `t`, `dir`, `isRTL`, and locale-aware formatters.

Stage Summary:
- Foundation is complete and self-consistent. All feature screens can import:
  - `useAppStore` from `@/lib/store` (state + actions + navigation).
  - `useI18n` from `@/hooks/use-i18n` (translations + formatters + RTL flag).
  - `getCourse`, `getInstructor`, `getQuizForCourse`, `roles`, `skills`, `searchCourses`, `promoCodes` from `@/lib/mock-data`.
  - Types from `@/lib/types`.
- Navigation is SPA-style: call `navigate("course-landing", { courseId: "c-sql" })`. The root `page.tsx` (built by main in Task ID 4) will switch screens based on `route.name`.
- Routes defined: `auth | onboarding | home | search | course-landing | course-player | my-learning | cart | checkout | checkout-confirmation | quiz`.
- Confetti: any component can call `useAppStore.getState().fireConfetti()` to trigger the global confetti overlay (built in Task ID 2). Onboarding, checkout-confirmation, and quiz-pass all fire confetti.
- Next: Task ID 2 (shared components + app shell), then parallel Task IDs 3-a..3-i for feature screens.

---
Task ID: 3-c
Agent: sub-agent (HomeScreen)
Task: Build the Home screen (Udemy-inspired discovery + Coursera goal orientation)

Work Log:
- Read worklog + foundation files: `src/lib/store.ts`, `src/lib/mock-data.ts`, `src/lib/types.ts`, `src/hooks/use-i18n.ts`, `src/lib/i18n.ts` (home.* / card.* / myLearning.* / brand.* keys), `src/components/shared/{CourseCard,CourseThumb,EmptyState}.tsx`, `src/components/ui/{button,progress,skeleton}.tsx`, `src/app/globals.css` (success/gold/no-scrollbar tokens).
- Created `/home/z/my-project/src/components/screens/HomeScreen.tsx` — NAMED export `HomeScreen`, `'use client'` at top.
- Skeleton loading: `useState(true)` + `useEffect` that calls `setTimeout(() => setLoading(false), 600)` with cleanup `clearTimeout`. setState is called inside the timeout callback (not synchronously in the effect body) to satisfy `react-hooks/set-state-in-effect`.
- Hero greeting: `t("home.greeting", { name: user.name })` + `t("home.greetingSub")`. Falls back to `t("brand.tagline")` (no subtitle) when there is no user.
- Continue learning section (3 modes):
  1. Zero enrolled courses → welcoming CTA card using `EmptyState` with `GraduationCap` icon, `myLearning.empty.inProgress` title, `home.greetingSub` description, and a primary Button → `navigate("search")` labelled with `confirmation.cta` ("Start learning").
  2. Enrolled but every course is 100% complete → slim success-tinted banner with `myLearning.completed` text and an outline button → `navigate("my-learning")`.
  3. Otherwise → horizontal carousel of custom `ContinueLearningCard` items (one per enrolled course with `getCourseProgress(id) < 100`). Each card: `CourseThumb` (showPlay) wrapped in a button → `navigate("course-landing", { courseId })`, title (line-clamp-2, locale-aware), instructor name, a custom progress bar (`h-2 bg-muted` track + `bg-success rounded-full` fill whose width is `${pct}%` — fills from start edge in both LTR and RTL because the parent inherits `dir`), `{n}% complete` (`myLearning.progress`) on the start side, total lesson count (`card.lessons`) on the end side, and a full-width primary "Resume" button (`myLearning.resume`) → `navigate("course-player", { courseId })`.
- Three discovery carousels, each a `<section aria-labelledby="…">` with a `SectionHeader` (icon chip + title + subtitle on the start side, ghost "View all" button with directional arrow on the end side → `navigate("search")` for the discovery sections, `navigate("my-learning")` for continue-learning). Carousels use `flex gap-4 overflow-x-auto no-scrollbar pb-2`; each card is wrapped in a `w-72 shrink-0` div. Cards render `<CourseCard course={c} variant="default" showActions />`.
  1. "What to learn next" (`home.whatNext` / `home.whatNextSub`): seeded from categories of enrolled ∪ `demoHistoryCourseIds`, excludes already-enrolled, top 10. First 2 spread with `bestseller: true` for display (original mock data untouched).
  2. "Recommended for you" (`home.recommended` / `home.recommendedSub`): from `onboardingAnswers.roleId` → `getRole` → `recommendedCourseIds`, minus enrolled. Falls back to top-rated non-enrolled courses when no role is set.
  3. "Trending now" (`home.trending` / `home.trendingSub`): courses sorted by `enrolledCount` desc, top 8, first 3 spread with `bestseller: true`.
- RTL/i18n: All text via `t()`. Logical CSS only (`ms-`, `me-`, `ps-`, `pe-`, `start-`, `end-`). The "View all" arrow flips between `ArrowRight` (LTR) and `ArrowLeft` (RTL) via `isRTL`. Progress-bar fill direction is inherited from `<html dir>` so it grows from the start edge in both dialects.
- Accessibility: every section is `<section aria-labelledby>`; `ContinueLearningCard` thumb is a real `<button>` with `aria-label={title}`; carousels are native `overflow-x-auto` so keyboard arrows + screen-reader browsing work out of the box.
- Sub-components kept in the same file (no new shared files needed): `SectionHeader`, `CarouselSection`, `ContinueLearningCard`, `HomeSkeleton`.
- Lint: `cd /home/z/my-project && bun run lint` → CLEAN (no errors, no warnings). Also confirmed `bunx tsc --noEmit` reports zero errors originating from `HomeScreen.tsx` (pre-existing errors in `examples/`, `skills/`, and `src/components/layout/Header.tsx` are from other agents' WIP and out of scope).

Stage Summary:
- `HomeScreen` is a NAMED export at `src/components/screens/HomeScreen.tsx`, ready for the root `page.tsx` (Task ID 4) to render when `route.name === "home"`.
- No foundation files were modified. No new shared components were added.
- Honors all spec requirements: 600ms skeleton, hero greeting, continue-learning rail with progress + Resume, three discovery carousels with View-all, empty-state CTA, RTL + Arabic, max-w-7xl container, no-scrollbar carousels, accessibility landmarks.

---
Task ID: 3-a
Agent: sub-agent (AuthScreen)
Task: Build the Auth screen — full-viewport entry with brand hero + auth card

Work Log:
- Read the worklog + foundation files first: `src/lib/store.ts` (auth methods `signUp(name,email)` → onboarding, `logIn(email)` → home with seeded progress, `loginAsDemo()` → home with full pre-seeded state, `setLocale`, `logOut`), `src/lib/types.ts` (`Locale = "en" | "ar"`, `RouteName`), `src/hooks/use-i18n.ts` (`useI18n()` → `{ t, locale, dir, isRTL, ... }`), `src/lib/i18n.ts` (existing auth.* + brand.* keys), `src/app/globals.css` (design tokens: `--primary #5624D0`, `--gold #F5C518`, `--success`, `.view-enter` keyframe, RTL font switching), and the shadcn UI primitives `button`, `card`, `input`, `label`, `tabs`.
- Added NEW i18n keys to ALL THREE dictionaries (en / arSa / arEg) in `src/lib/i18n.ts` (inserted right after the existing `auth.passwordPlaceholder` block in each dict, so the foundation file's structure is preserved — only new keys appended, no existing key touched):
  - `auth.or` — the "or" divider label.
  - `auth.demoHelper` — the helper line under the demo button ("Pre-loaded with 2 enrolled courses, in-progress lessons, and a saved wishlist…").
  - `auth.trust.goals` + `auth.trust.goalsDesc` — "Goal-driven paths" / "Personalized curriculum built around your role."
  - `auth.trust.ai` + `auth.trust.aiDesc` — "AI tutor in every course" / "Ask, summarize, and get unstuck in seconds."
  - `auth.trust.certificates` + `auth.trust.certificatesDesc` — "Certificates that count" / "Shareable proof of skills employers trust."
  - `auth.error.nameRequired`, `auth.error.emailRequired`, `auth.error.emailInvalid`, `auth.error.passwordShort` — inline validation messages.
  - `auth.languageToggle`, `auth.language.en` ("EN"), `auth.language.ar` ("ع") — language switcher labels.
  - `auth.demoBadge` — "Recommended for a quick tour" pill above the demo CTA.
  - `auth.tabsLabel`, `auth.showPassword`, `auth.hidePassword` — accessible names for the tablist and the password visibility toggle (localized for screen readers).
  - Saudi and Egyptian tones both handled (e.g. Saudi "يلا نبدأ كطالب تجريبي" / Egyptian "يلا بينا كطالب تجريبي" come from the existing `auth.demoCta` key; new keys mirror the dialect split).
- Created `/home/z/my-project/src/components/screens/AuthScreen.tsx` — NAMED export `AuthScreen`, `'use client'` at top. Self-contained (no new shared components needed; three small private sub-components `Field`, `PasswordInput`, `LanguageSwitcher`/`SwitchButton` live in the same file).
- Layout: full-viewport `flex flex-col lg:flex-row` root with the `.view-enter` entrance animation class. Left/top = brand hero `<section>`; right/bottom = `<main>` with the auth `<Card>`. On mobile the hero stacks on top (compact `p-6 sm:p-10`) and the card sits below; on `lg+` the hero takes `flex-1 lg:min-h-screen` and the card centers in the remaining space.
- Brand hero: violet gradient background `linear-gradient(135deg, #5624D0 → #4b1ec0 → #2d1a8a)` with two decorative overlays (a `radial-gradient` dot pattern at 14% opacity + a blurred white glow blob in the top-end corner), both `aria-hidden`. Content: logo chip (`GraduationCap` in a `bg-white/15 ring-1 ring-white/25` rounded square) + `brand.name`; then `auth.title` (h1, `text-3xl→text-5xl`) + `auth.subtitle` + `brand.tagline`; then a 3-item trust list using `Target` / `Sparkles` / `Award` icons in white-on-15% chips with title + description.
- Auth card: compact brand lockup (violet `bg-primary` logo chip + `brand.name` + `brand.tagline`) at the top for mobile context, then a shadcn `Tabs` with `Sign up` (default) / `Log in` triggers in a 2-column `TabsList` (`aria-label={t("auth.tabsLabel")}` — Radix provides the `tablist`/`tab`/`tabpanel` roles automatically). Each `TabsContent` renders a `<form>` (Enter-to-submit via `onSubmit`, `noValidate` to use our own messages).
  - Sign up form: `Full name` (`auth.name` + `auth.namePlaceholder`), `Email` (`auth.email` + `auth.emailPlaceholder`), `Password` (`auth.password` + `auth.passwordPlaceholder`) with a show/hide eye toggle, then a full-width primary `Create account` button (`auth.signupCta`) → calls `signUp(name.trim(), email.trim())`.
  - Log in form: `Email` + `Password` (same toggle) + full-width `Log in` button (`auth.loginCta`) → calls `logIn(email.trim())`.
  - Validation: non-empty name (signup only), email-shaped (`/^[^\s@]+@[^\s@]+\.[^\s@]+$/`), password ≥ 4 chars. Errors render as `role="alert"` paragraphs linked via `aria-describedby`, and inputs get `aria-invalid`. Switching tabs clears stale errors.
  - Field IDs are generated with `useId()` so label/`htmlFor` associations are stable and SSR-safe.
- Below the tabs: a centered "or" divider (`auth.or`) flanked by two border lines with the label sitting on a `bg-card` chip so the line is visually broken.
- Demo CTA: a gold-tinted `auth.demoBadge` pill (Star icon) above a prominent outline button styled with `border-2 border-primary/40 bg-primary/5 text-primary hover:bg-primary/10` + `Sparkles` icon, labelled `auth.demoCta`, calling `loginAsDemo()`. Under it, the `auth.demoHelper` line in muted text.
- Footer: `auth.terms` in muted, centered text.
- Language switcher: absolute `end-3 top-3 sm:end-5 sm:top-5 z-30`, a `bg-card/85 backdrop-blur` pill with a `Globe` icon and two `aria-pressed` toggle buttons `EN` / `ع` wired to `setLocale("en" | "ar")`. Positioned with logical `end-*` so it lands top-right in LTR and top-left in RTL automatically. Visible on both the dark hero (light pill) and the light card area.
- RTL/i18n: every visible string + every aria-label goes through `t()`. Layout uses only logical CSS (`ps-`, `pe-`, `ms-`, `me-`, `start-`, `end-`, `text-start`). The password eye toggle sits at `end-0` with the input padded `pe-10`, so it flips correctly in Arabic. `<html dir>` is synced by the existing `DirSync` component, so no dir handling is needed inside the screen.
- No `useEffect` is used anywhere (validation + tab switches are all event-handler driven), so the `react-hooks/set-state-in-effect` rule is satisfied by construction.
- Lint: `cd /home/z/my-project && bun run lint` → CLEAN (no errors, no warnings). `bunx tsc --noEmit` reports zero errors originating from `AuthScreen.tsx` or the `i18n.ts` edits (the only remaining tsc errors are pre-existing ones in `examples/`, `skills/`, and `src/components/layout/Header.tsx` — out of scope for this task).

Stage Summary:
- `AuthScreen` is a NAMED export at `src/components/screens/AuthScreen.tsx`, ready for the root `page.tsx` (Task ID 4) to render when `route.name === "auth"` (which is the store's initial route).
- No protected foundation files were modified except `src/lib/i18n.ts`, where new keys were *appended* to all three dictionaries (en / arSa / arEg) without altering any existing key or its value.
- Honors all spec requirements: full-viewport split layout (brand hero + auth card), mobile stack, Sign up / Log in tabs with inline validation, `signUp`/`logIn`/`loginAsDemo` wired to the store, "or" divider, prominent demo CTA with helper line, terms footer, EN/ع language switcher in the top corner, logical-CSS RTL support, accessible labels/tablist/aria-invalid, and the `.view-enter` entrance animation.

---
Task ID: 3-d
Agent: search-screen
Task: SearchScreen — Coursera-inspired semantic search with filters sidebar

Work Log:
- Read foundation files: store.ts, mock-data.ts (searchCourses/getCourse/getRole + SearchResult type), i18n.ts (search.* + common.* keys), types.ts (Course, CourseLevel), use-i18n.ts, CourseCard.tsx, EmptyState.tsx, and shadcn/ui primitives (input, button, checkbox, label, radio-group, separator, badge, skeleton, sheet).
- Created `/home/z/my-project/src/components/screens/SearchScreen.tsx` (named export `SearchScreen`), a single client component with sub-components `BrowseResults`, `SearchResults`, and `ResultsSkeleton` for clarity.
- Search input: large h-12/h-14 Input with leading SearchIcon, aria-label, type="search", and a trailing clear (X) button when query is non-empty. Wrapped in a `<form role="search">` so Enter submits and immediately commits the query (debounce timer then no-ops).
- Debounced search: a single `useEffect` keyed on `query` schedules a `window.setTimeout` (250ms) whose *callback* calls `setCommittedQuery` + `setResults(searchCourses(query))`. Cleanup clears the timer. No setState in the effect body — keeps `react-hooks/set-state-in-effect` happy. The `loading` flag is *derived* (`query.trim() !== committedQuery.trim()`), which doubles as the ~250ms skeleton window.
- Browse mode (empty committed query): shows all courses + a "Browse by category" chip row (Data / Web Dev / Design / Product / Marketing) with EN+AR labels. Clicking a chip toggles a `selectedCategory` filter; an active chip is shown as a removable Badge.
- Search mode (non-empty query): shows `search.resultsFor` ({q}) heading + `search.count` ({n}). When the first visible (filtered) result has a `matchedRole`, a subtle violet-tinted banner renders `search.semantic` + ": " + role label (looked up via `getRole`, locale-aware).
- No-results state: `EmptyState` with `SearchIcon`, `search.noResults` title, `search.suggested` description, and the 8 suggested keywords (SQL, React, data analyst, become a data analyst, relational databases, figma, product manager, SEO) as clickable chips that fill the search input on click.
- Filters panel: shared `renderFilterPanel(prefix)` closure rendered in both the desktop `<aside>` (hidden lg:block, w-64 shrink-0, sticky top-20) and the mobile Sheet. Each section uses `<fieldset>/<legend>` for accessibility. Level = Checkbox group (Beginner/Intermediate/Advanced with AR translations). Duration = RadioGroup (any/short/medium/long). Rating = RadioGroup (any/4.0+/4.5+). Language = Checkbox (English). IDs are prefixed (`desktop-` / `mobile-`) to avoid duplicate-DOM-id collisions when both trees are mounted.
- Clear-filters button appears in the desktop sidebar header, inside the mobile filter panel, and inline on mobile next to the Filters trigger. `activeFilterCount` powers a numeric Badge on the mobile Filters button.
- Mobile filters: a `Sheet` (`side={isRTL ? "right" : "left"}`) with SheetTitle + sr-only SheetDescription (silences Radix warning), scrollable filter body, and a footer with optional Clear + Close (`common.close`) buttons. Filters apply live so the Sheet's Close just dismisses the drawer.
- Results grid: responsive `grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-4` of `<CourseCard variant="default" showActions />`. Skeleton uses 6 placeholder cards with `Skeleton` blocks (aspect-video thumb + meta lines) while `loading` is true.
- Layout: `max-w-7xl mx-auto px-4 sm:px-6 py-6 sm:py-8` container; flex with sidebar + main (flex-1 min-w-0). Logical CSS throughout (`ps-`/`pe-`/`start-`/`end-`/`ms-`/`me-`) so RTL flips correctly.
- Accessibility: search input has aria-label, the form has `role="search"`, filter sections use fieldset/legend, results region has `aria-live="polite"` + `aria-busy={loading}`, category chips use `aria-pressed`.

Stage Summary:
- SearchScreen is complete and exported from `@/components/screens/SearchScreen`.
- `cd /home/z/my-project && bun run lint` → CLEAN (no warnings, no errors).
- tsc --noEmit shows zero errors in this file (pre-existing errors in `examples/`, `skills/`, and `Header.tsx`'s `useMobile` import are out of scope and unrelated).
- Navigation: clicking a `CourseCard` calls `navigate("course-landing", { courseId })` (handled inside CourseCard).
- The screen wires up cleanly to the foundation: uses `useI18n()` for t/locale/isRTL, `searchCourses`/`getRole`/`SearchResult` from mock-data, `CourseCard` + `EmptyState` shared components, and shadcn/ui Input/Button/Checkbox/Label/RadioGroup/Separator/Badge/Skeleton/Sheet.
- No new routes, no localStorage, no backend, no edits to foundation files. Only one new file: `src/components/screens/SearchScreen.tsx`.

---
Task ID: 3-b
Agent: sub-agent (OnboardingScreen)
Task: Build the Onboarding screen — Coursera-inspired 5-step personalization flow

Work Log:
- Read the worklog + foundation files first: `src/lib/store.ts` (onboarding slice: `onboardingStep` 0..4, `onboardingAnswers`, `setOnboardingAnswers(patch)`, `nextOnboardingStep`/`prevOnboardingStep`/`setOnboardingStep`, `completeOnboarding()` auto-enrolls 2 recommended courses + routes home, `fireConfetti()`, `user` for greeting), `src/lib/types.ts` (`OnboardingAnswers`, `Role`, `Skill`), `src/hooks/use-i18n.ts` (`useI18n()` → `{ t, locale, dialect, dir, isRTL, formatNumber, … }`), `src/lib/i18n.ts` (all `onboarding.*` keys already exist in en / arSa / arEg — `onboarding.title`, `onboarding.subtitle`, `onboarding.step` with `{current}/{total}`, `onboarding.saveExit`, `onboarding.back`, `onboarding.next`, `onboarding.finish`, `onboarding.s1.*` … `onboarding.s5.*`, `onboarding.s5.confetti`), `src/lib/mock-data.ts` (`roles` with `icon` lucide-name string + `recommendedCourseIds` + `skillIds`, `getRole`/`getSkill`/`getCourse`, `courses`), `src/components/shared/StepProgressBar.tsx` (`<StepProgressBar current total labels />`, 0-indexed `current`), `src/components/shared/CourseCard.tsx` (default `showActions=false`), and shadcn primitives `button`/`input`/`skeleton` + the `.slide-enter` / `.typing-dot` keyframes in `src/app/globals.css`.
- Created `/home/z/my-project/src/components/screens/OnboardingScreen.tsx` — NAMED export `OnboardingScreen`, `'use client'` at top. Single file with five private sub-components (`IntentStep`, `RoleStep`, `SkillsStep`, `EducationStep`, `CompletionStep`) co-located for clarity; no new shared files needed.
- Layout: full-viewport `<main>` with `bg-gradient-to-b from-accent/40 via-background to-background`. Centered `max-w-4xl` container. Top = `<header>` with `onboarding.title` + `onboarding.subtitle` (+ optional `nav.welcome, {name}` if `user` is set) on the start side and a ghost `onboarding.saveExit` button on the end side → calls `completeOnboarding()` to skip to home. Below: `<StepProgressBar current={step} total={5} labels={stepLabels} />` where `stepLabels` is a shortened locale-aware inline array (`["Goal","Role","Skills","Education","Plan"]` in EN, `["الهدف","الدور","المهارات","التعليم","الخطة"]` in AR — kept inline because `i18n.ts` is a protected foundation file). The step container is `<div key={step} className="slide-enter">` so React remounts on step change and replays the slide-in animation. Footer = sticky-ish row with Back (ghost, disabled on step 0) on the start side and the contextual Next / Build my plan / Start learning CTA on the end side, plus a hidden-on-mobile `onboarding.step` ({current}/{total}) counter.
- Step 0 — Intent (`onboarding.s1.*`): two large selectable cards side-by-side (`grid sm:grid-cols-2`), `TrendingUp` for "Improve existing skills" (`onboarding.s1.improve` + `onboarding.s1.improveDesc`) and `Rocket` for "Learn something new" (`onboarding.s1.learnNew` + `onboarding.s1.learnNewDesc`). Each card is a `<button aria-pressed>` with a 12×12 icon chip (filled violet when active, accent otherwise), title + description, and a circular violet check badge in the `end-4 top-4` corner when selected. Selected card gets `border-primary ring-2 ring-primary/20`. Writes `setOnboardingAnswers({ intent })`.
- Step 1 — Role (`onboarding.s2.*`): a `relative` Input with a leading `Search` icon at `start-3` and `ps-9` padding, placeholder `onboarding.s2.search`, filters `roles` by label (EN+AR, case-insensitive substring). Grid `sm:grid-cols-2 lg:grid-cols-3` of role cards; each card looks up `ROLE_ICONS[role.icon]` (a `Record<string, LucideIcon>` mapping `BarChart3`/`Code2`/`Layers`/`PenTool`/`Briefcase`/`Megaphone`/`BrainCircuit`/`PieChart` → component, fallback `Briefcase`), shows locale-aware label (`locale === "ar" ? labelAr : label`), and a "{n} skills / مهارات" count via `formatNumber(skillCount)` (with singular/plural word choice per locale). Selecting writes `setOnboardingAnswers({ roleId, skillIds: [] })` to reset skills. Empty-search state shows a muted "No roles match your search." line.
- Step 2 — Skills (`onboarding.s3.*`): looks up `getRole(roleId)` → maps `role.skillIds` → `getSkill` (filtered via a `s is NonNullable<typeof s>` type predicate). Renders a `flex flex-wrap gap-2` of pill buttons (`rounded-full border-2 px-4 py-2`) that fill `border-primary bg-primary text-primary-foreground` + a `Check` glyph when selected. Defensively handles missing `roleId` with a "Please pick a role in the previous step first." prompt. The Next button is intentionally always clickable on this step so we can show the inline `onboarding.s3.none` hint (`role="alert"`, destructive color) when the user tries to advance with 0 skills — `setShowSkillHint(true)` is fired from the parent's `handleNext` (not from an effect), and cleared on any toggle or on navigation.
- Step 3 — Education (`onboarding.s4.*`): six selectable cards (`grid sm:grid-cols-2 lg:grid-cols-3`) for High school / Associate / Bachelor's / Master's / PhD / Self-taught. Education labels + descriptions are kept inline in a small bilingual `EDUCATION_LEVELS` table (with `GraduationCap` icon for formal degrees and `Sparkles` for Self-taught) — this avoids modifying `src/lib/i18n.ts`, which is a protected foundation file. Each card is a `<button aria-pressed>` with an 8×8 icon chip, label, description, and a circular check badge in the `end-2 top-2` corner. Writes `setOnboardingAnswers({ educationLevel })`. This step's primary CTA flips to `onboarding.finish` ("Build my plan") with a `Sparkles` icon; clicking it calls `goToCompletion()` which resets `planReady` to false (in the click handler, NOT in an effect) and then `nextOnboardingStep()`s into step 4.
- Step 4 — Completion (`onboarding.s5.*`): a `useEffect` keyed on `step` short-circuits unless `step === 4`, then (a) fires `useAppStore.getState().fireConfetti()` to trigger the global `<ConfettiOverlay/>` burst, and (b) schedules `window.setTimeout(() => setPlanReady(true), 1200)` with a `clearTimeout` cleanup. **setState is only called inside the setTimeout callback — never synchronously in the effect body** — so the `react-hooks/set-state-in-effect` rule stays happy. For the first ~1.2s the screen shows a centered success check (`CheckCircle2` in a `bg-success/15 text-success` 16×16 circle), `onboarding.s5.title`, `onboarding.s5.subtitle`, three `.typing-dot` bouncing dots, and a 2×2 grid of `Skeleton` placeholders (h-24 thumb + 3 lines) to mimic "Building your personalized path…". After the timer fires, the subtitle swaps to `onboarding.s5.confetti` ("Path ready!") and the skeleton grid is replaced by the `onboarding.s5.recommended` heading + a `grid sm:grid-cols-2` of `<CourseCard>` items. The recommended list = `getRole(roleId).recommendedCourseIds.map(getCourse).filter(Boolean)`; if no role was chosen (or the role has no recommended courses) it falls back to top-trending by `enrolledCount` and a muted "Showing trending courses because no role was selected." footnote. The footer's primary CTA on this step is a `size="lg"` `onboarding.s5.startLearning` button (`Rocket` icon) — `disabled={!planReady}` until the shimmer resolves — which calls `completeOnboarding()` → store auto-enrolls the 2 first recommended courses + routes to home.
- Validation: `canProceed = useMemo(...)` returns `Boolean(answers.intent)` on step 0, `Boolean(answers.roleId)` on step 1, `true` on step 2 (so the hint can fire), `Boolean(answers.educationLevel)` on step 3, `true` on step 4. Back/Next icons flip with `isRTL` (`NextIcon = isRTL ? ArrowLeft : ArrowRight`, `BackIcon` mirrored) so "forward" visually points the right way in Arabic.
- RTL/i18n: every visible string + every `aria-label` goes through `t()`. Layout uses only logical CSS (`ps-`, `pe-`, `ms-`, `me-`, `start-`, `end-`, `text-start`) so the icon-chip / check-badge / search-icon positions all flip automatically in Arabic. `<html dir>` is synced by the existing `DirSync` component, so no dir handling is needed inside the screen. Saudi vs Egyptian tones both flow through the same `t()` (e.g. `onboarding.s5.startLearning` is "يلا نبدأ" in SA and "يلا نبدأ" in EG per the existing dict).
- Accessibility: each step is a `<section aria-label={t("onboarding.sN.title")}>`. Every selectable card/chip is a real `<button type="button" aria-pressed={isActive}>`. The skills hint uses `role="alert"`. The role search input has `aria-label`. The progress bar exposes its current step text + a sr-only percentage via `StepProgressBar`. Color is never the only signal — every selected state also gets a `Check` glyph + a `ring-2 ring-primary/20` halo.
- Lint: `cd /home/z/my-project && bun run lint` → CLEAN (no errors, no warnings). `bunx tsc --noEmit` reports zero errors originating from `OnboardingScreen.tsx` (the only remaining tsc errors are pre-existing ones in `examples/`, `skills/`, and `src/components/layout/Header.tsx` — out of scope for this task).

Stage Summary:
- `OnboardingScreen` is a NAMED export at `src/components/screens/OnboardingScreen.tsx`, ready for the root `page.tsx` (Task ID 4) to render when `route.name === "onboarding"`.
- No foundation files were modified. No new shared components were added. Only one new file created.
- Honors all spec requirements: 5-step flow with slide transitions, StepProgressBar with shortened labels, "Save & continue later" skip link, Back/Next with disabled-until-valid + per-step hint, RTL + Saudi/Egyptian Arabic, role-icon lookup, skills multi-select with inline none-hint, education cards, completion step with confetti + 1.2s shimmer + recommended CourseCards + trending fallback, `completeOnboarding()` wiring, full accessibility (semantic landmarks, aria-pressed, aria-label, role="alert"), and the `react-hooks/set-state-in-effect` rule satisfied by routing all completion-step setState through setTimeout callbacks.

---
Task ID: 3-g
Agent: sub-agent (MyLearningScreen)
Task: Build the My Learning screen — enrolled courses with progress, saved wishlist, and certificates

Work Log:
- Read the worklog + foundation files first: `src/lib/store.ts` (`enrolledCourseIds`, `savedCourseIds`, `certificates` array of `{ id, courseId, issuedOn, score }`, `getCourseProgress(courseId)` → 0..100, `navigate(name, params)`, `toggleSaved`, `addToCart`, `pushToast`, `user`), `src/hooks/use-i18n.ts` (`useI18n()` → `{ t, locale, isRTL, formatNumber, formatPrice, formatDate, ... }`), `src/lib/types.ts` (`Certificate`, `Course`, `RouteName` includes `my-learning`), `src/lib/mock-data.ts` (`getCourse`, `getInstructor`), `src/lib/i18n.ts` (existing `myLearning.*`, `card.*`, `cart.*`, `landing.*`, `brand.*`, `quiz.*`, `home.greetingSub` keys), `src/components/shared/{CourseThumb,EmptyState}.tsx`, and shadcn UI primitives `tabs`, `card`, `button`, `badge`, `separator`, `dialog`.
- Did NOT modify any foundation file (incl. `src/lib/i18n.ts`). For the handful of UI labels without a dedicated i18n key ("Move to cart", "Score", "This is to certify that", "has successfully completed", "Signature", "Official seal", "Review course"→used `card.goToCourse` instead, toast strings), used an inline locale helper `L(en, ar)` bound to the active `locale`.
- Created `/home/z/my-project/src/components/screens/MyLearningScreen.tsx` — NAMED export `MyLearningScreen`, `'use client'` at top. Self-contained (4 small private sub-components `CountPill`, `SavedCourseCard`, `CertificateCard`, `CertificateDocument` live in the same file).
- Layout: `mx-auto max-w-7xl px-4 sm:px-6 py-6 sm:py-8` container. Header = `<h1>` (`myLearning.title`) + muted subtitle (`home.greetingSub`, since no `myLearning.subtitle` key exists — chosen because it fits the "pick up where you left off" semantics perfectly and is already localized for both Arabic dialects).
- Tabs: controlled via `useState<TabValue>("in-progress")` so the "View certificate" link on a completed course in tab 1 can programmatically switch to tab 3 via `setTab("certificates")`. `TabsList` has `aria-label={t("myLearning.title")}` (Radix provides `tablist`/`tab`/`tabpanel` roles). Each `TabsTrigger` shows the label + a `CountPill` (violet-tinted chip with the tab's item count; hidden when count is 0). TabsList uses `h-auto flex-wrap` so the count pills don't clip on narrow mobile.
- TAB 1 — In progress (`myLearning.tab.inProgress`):
  - Responsive grid `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 sm:gap-6`.
  - Each card = `Card` (`gap-0 p-0 overflow-hidden`, hover lift). CourseThumb (with `showPlay`) is wrapped in a real `<button type="button">` with `aria-label={title}` → `navigate("course-player", { courseId })`, with a `focus-visible:ring` for keyboard users.
  - Below thumb: locale-aware title (line-clamp-2) + instructor name (locale-aware via `instructor.nameAr ?? instructor.name`).
  - Progress: custom div bar (NOT shadcn `Progress`, because its `translateX` indicator fills from the left edge regardless of `dir`, which is wrong in RTL). Used `h-2 w-full bg-muted rounded-full overflow-hidden` track + inner `div` with `style={{ width: \`${pct}%\` }}` so the fill grows from the inline-start edge in both LTR and RTL (block boxes align to inline-start). Fill color is `bg-primary` while in progress and `bg-success` when 100%. The bar has `role="progressbar"` + `aria-valuenow={pct}` + `aria-valuemin={0}` + `aria-valuemax={100}` + `aria-label`.
  - Label row: if `pct === 0` → `myLearning.notStarted` (muted); else `myLearning.progress` with `{n: pct}`. If `pct === 100` → green `myLearning.completed` Badge with `CheckCircle2` icon.
  - CTA: full-width primary Button. If `pct < 100` → `myLearning.resume` with `PlayCircle` icon → `navigate("course-player", { courseId })`. If 100 → `card.goToCourse` ("Go to course" / "افتح الكورس", used as the closest localized match for "Review course") with `RotateCcw` icon → same navigation. When the course is 100% AND a certificate exists for it, a secondary outline Button with `Award` icon appears (label `myLearning.certificate.view` on sm+, icon-only on mobile) → `setTab("certificates")`.
  - Empty: `EmptyState` with `GraduationCap` icon, `myLearning.empty.inProgress` title, `home.greetingSub` description, and a "Browse courses" Button (`myLearning.browse`) → `navigate("search")`.
- TAB 2 — Saved (`myLearning.tab.saved`):
  - Same responsive grid. `SavedCourseCard` sub-component: CourseThumb wrapped in a `<button>` → `navigate("course-landing", { courseId })`. Below: title + instructor + price (`formatPrice(course.price)`, with `originalPrice` strikethrough if present). Two action buttons: primary "Move to cart" (`ShoppingCart` + inline `L("Move to cart","انقل للسلة")`) → `addToCart(courseId)` + `toggleSaved(courseId)` (removes from saved — true "move" semantics) + success toast (`{ title: "Moved to cart"/"اتنقل للسلة", description: courseTitle, variant: "success" }`); and an icon-only outline "Remove" button (`Trash2`, `aria-label={t("cart.remove")}`) → `toggleSaved(courseId)` + default toast.
  - Empty: `EmptyState` with `Heart` icon, `myLearning.empty.saved`, browse button.
- TAB 3 — Certificates (`myLearning.tab.certificates`):
  - Grid of `CertificateCard`s. Each card has a gold theme: `border-2 border-gold/50`, a `bg-gradient-to-r from-gold/0 via-gold to-gold/0` top accent line, and a `from-card via-accent/20 to-card` background gradient. Header row: a violet `GraduationCap` in a `ring-gold/60` circle + `brand.name` wordmark on the start side, a gold `Award` + `myLearning.completed` badge on the end side. Body: `landing.includes.certificate` label ("Certificate of completion") in uppercase tracking, the locale-aware course title, and `myLearning.certificate.issued` with `formatDate(issuedOn)`. Footer: `Score` label + `score%` Badge. Two buttons: outline "View" (`Eye` + `myLearning.certificate.view`) → opens Dialog; default "Download" (`Download` + `myLearning.certificate.download`) → success toast "Downloading certificate…"/"جاري تحميل الشهادة…".
  - Empty: `EmptyState` with `Award` icon, `myLearning.empty.certificates`, browse button.
- Certificate Dialog: controlled by `viewingCert` state (`open={viewingCert !== null}`, `onOpenChange` clears it). `DialogContent` has `aria-label={t("myLearning.certificate.view")}` plus an sr-only `DialogTitle` + `DialogDescription` (Radix requires a title for the accessible name; both are visually hidden but provide the name/description). Renders `CertificateDocument` — a larger certificate layout: `border-4 border-gold/60` frame on a `from-background via-accent/20 to-background` gradient. Header = `GraduationCap` in a `ring-gold` circle + `brand.name` + `brand.tagline`. Two `Separator`s with `bg-gold/40` divide three sections. Body = `landing.includes.certificate` heading, "This is to certify that"/"تشهد منصة ماستري بأن", the student name (`user.name` with "Learner"/"متعلّم" fallback) in large bold, "has successfully completed"/"أتمّ بنجاح دورة", and the course title in `text-primary`. Footer = 3-column grid: issue date + score on the start side, a `Sparkles` seal in a gold ring centered, and a signature line (`border-t-2 border-foreground`) on the end side.
- RTL / i18n: every visible string + every aria-label goes through `t()` or the inline `L(en, ar)` helper. Layout uses only logical CSS (`ms-`, `me-`, `ps-`, `pe-`, `text-start`, `items-end`). The progress-bar fill direction is inherited from `<html dir>` (set by the existing `DirSync` component) so it grows from the start edge in both dialects. The certificate dialog's 3-column footer uses `grid-cols-3` with `items-end` and `flex-col items-end` for the signature column, which mirrors correctly under RTL.
- No `useEffect` used anywhere — all state transitions happen in event handlers (tab clicks, button clicks, dialog open/close), so the `react-hooks/set-state-in-effect` rule is satisfied by construction. `useMemo` is used only for derived lists (`enrolledCourses`, `savedCourses`, `certsWithCourses`) with stable dependencies.
- Store actions (`addToCart`, `toggleSaved`, `pushToast`, `navigate`, `getCourseProgress`) are subscribed at the top of the component via `useAppStore((s) => s.xxx)` selectors, so re-renders stay scoped.
- Accessibility: tabs have `aria-label` on the list (Radix provides the rest); each clickable thumbnail is a real `<button>` with `aria-label` + `focus-visible:ring`; progress bars expose `role="progressbar"` + `aria-valuenow/min/max` + `aria-label`; the remove button is icon-only with `aria-label`; the dialog has `aria-label` + sr-only title/description; the certificate "View" link on completed courses is a keyboard-reachable Button.
- Lint: `cd /home/z/my-project && bun run lint` → CLEAN (no errors, no warnings). `bunx tsc --noEmit` reports zero errors originating from `MyLearningScreen.tsx` (the only remaining tsc errors are pre-existing ones in `examples/`, `skills/`, and `src/components/layout/Header.tsx` — out of scope for this task).

Stage Summary:
- `MyLearningScreen` is a NAMED export at `src/components/screens/MyLearningScreen.tsx`, ready for the root `page.tsx` (Task ID 4) to render when `route.name === "my-learning"`.
- No foundation files were modified. No new shared components were added. One new file created.
- Honors all spec requirements: header + subtitle, 3 tabs with live counts, in-progress grid (thumb→player, progress bar + %, not-started/completed states, Resume vs Review-course CTA, view-certificate cross-tab link), saved grid (thumb→landing, move-to-cart with toast + auto-remove, remove with toast, price formatting), certificates grid (gold-bordered certificate cards with seal + score + View/Download), certificate View Dialog with larger document layout (student name from `user.name`, issue date, score, signature line, Edify seal), three empty states with browse CTA, RTL + Arabic (both dialects via inline `L()` helper for the few keys not in the dictionary), logical CSS throughout, full accessibility (tablist/tab/tabpanel roles, progressbar ARIA, keyboard-reachable buttons, dialog accessible name), and `react-hooks/set-state-in-effect` satisfied (no useEffect).

---
Task ID: 3-i
Agent: sub-agent (QuizScreen)
Task: Build the Quiz screen — multi-step one-question-per-screen flow with confetti on pass

Work Log:
- Read the worklog + foundation files first: `src/lib/store.ts` (quiz slice: `quizState: null | { quizId, courseId, answers: Record<questionId, number>, current, submitted, score }`, `startQuiz(quizId, courseId)` resets to fresh attempt, `answerQuizQuestion(questionId, index)` mutates answers, `nextQuizQuestion`/`prevQuizQuestion` bump `current` (prev clamped at 0), `submitQuiz(total, correct, passing)` sets `submitted=true` + `score=round(correct/total*100)`, AND when `score >= passing` it marks all course lessons complete + issues a certificate + calls `fireConfetti()`; `resetQuiz()` resets answers/current/submitted/score but keeps quizId; `route.params.courseId` is the entry point; `navigate("course-player", { courseId })` is the back path), `src/lib/types.ts` (`Quiz { id, title, titleAr?, courseId, sectionId?, lessonId?, passingScore: number, questions: QuizQuestion[] }`, `QuizQuestion { id, question, questionAr?, options[], optionsAr?, correctIndex, explanation, explanationAr? }`, `Locale = "en" | "ar"`, `RouteName` includes `"quiz"`), `src/hooks/use-i18n.ts` (`useI18n()` → `{ t, locale, isRTL, formatNumber, ... }`), `src/lib/mock-data.ts` (only one quiz wired today: `q-sql` for `c-sql`, 5 questions, passingScore 60; `getQuizForCourse(courseId)` returns `Quiz | undefined`), `src/lib/i18n.ts` (existing keys `quiz.title`, `quiz.question {current}/{total}`, `quiz.next`, `quiz.submit`, `quiz.retry`, `quiz.review`, `quiz.passed`, `quiz.failed`, `quiz.score`, `quiz.correct`, `quiz.incorrect`, `quiz.explanation`, `quiz.backToCourse`, `quiz.confetti`; reused `onboarding.back` for the Previous button label), `src/components/shared/StepProgressBar.tsx` (`current` 0-indexed, `total`, optional `labels`), `src/components/shared/EmptyState.tsx`, shadcn primitives `button`/`card`/`radio-group`/`label`/`progress`/`badge`/`separator`, `globals.css` tokens (`--primary #5624D0`, `--success #1E7F45`, `--destructive #B91C1C`, `--gold #F5C518`), and the `.slide-enter` keyframe (RTL-aware, defined for both `ltr` and `rtl`).
- Created `/home/z/my-project/src/components/screens/QuizScreen.tsx` — NAMED export `QuizScreen`, `'use client'` at top. Single file with four private sub-components (`BackToCourseButton`, `AnsweringView`, `ResultsView`, `ReviewList`) co-located for clarity; no new shared files needed.
- Inline bilingual helpers (kept in the file because `src/lib/i18n.ts` is a protected foundation file we may not edit): `STR` map for static strings (`certEarned` = "You earned a certificate!" / "حصلت على شهادة!", `failHint`, `hideReview`, `yourAnswer`, `correctAnswer`, `noQuizDesc`, `passingLabel`, `loading`), a `pick(locale, map)` selector, an `ofTotalLine(n, total, locale)` composer, and `localizeQuestion(q, locale)` / `localizeQuizTitle(q, locale)` helpers that respect the `?? fallback` pattern from the spec (`locale === "ar" ? q.questionAr ?? q.question : q.question`).
- **Entry + auto-start logic.** `QuizScreen` reads `courseId` from `useAppStore((s) => s.route).params?.courseId`, looks up `quiz = getQuizForCourse(courseId)`. A single `useEffect` (deps: `quiz`, `quizState?.quizId`, `courseId`, `startQuiz`) calls `startQuiz(quiz.id, courseId)` only when `!quizState || quizState.quizId !== quiz.id`. Because `startQuiz` is a Zustand store action (not a React `setState`), the call inside the effect body does not violate `react-hooks/set-state-in-effect`. The ID guard prevents infinite re-runs: after the first commit `quizState.quizId === quiz.id` and the effect short-circuits; `resetQuiz()` (Retry button) keeps the same `quizId`, so it never re-triggers `startQuiz`.
- **No-quiz branch.** If `getQuizForCourse(courseId)` returns undefined, render `<EmptyState>` with the `ClipboardList` icon, `t("quiz.title")` as the title, the localized `noQuizDesc` description, and a primary `t("quiz.backToCourse")` button → `navigate("course-player", { courseId })`. A `BackToCourseButton` ghost link sits above it for symmetry.
- **Loading placeholder.** While `quizState` is null or still pointed at a different quiz (one frame before the effect commits), render a small `aria-busy` skeleton (heading bar + progress bar + 64-rem card) with a sr-only `loading` string. No layout jump.
- **Header.** Above both phases: a `BackToCourseButton` ghost link (RTL-aware arrow), then a `<header>` with a `ClipboardList` Badge + `t("quiz.title")`, a muted `"{passingLabel}: {passingScore}%"` line, and the locale-aware quiz title (`titleAr ?? title`) as an `h1` at `text-2xl sm:text-3xl`.
- **Answering phase (`AnsweringView`).** Shown when `!quizState.submitted`:
  - `<StepProgressBar current={quizState.current} total={quiz.questions.length} />` (0-indexed `current`, matches the StepProgressBar contract) followed by `t("quiz.question", { current: current + 1, total })` ("Question 1 of 5").
  - Question card wrapped in `<div key={current} className="slide-enter">` so React remounts on each navigation and the `.slide-enter` keyframe (280ms slide-in, RTL variant flips the translateX sign) replays per question. The Card carries `aria-label={questionCounter}` so screen readers announce "Question 2 of 5" when focus lands.
  - `<CardHeader>` renders the locale-aware question text (large `text-lg sm:text-xl`) as `CardTitle`, with a sr-only `CardDescription` echoing the counter.
  - `<CardContent>` holds a shadcn `<RadioGroup>` (Radix provides the `radiogroup`/`radio` roles automatically) bound to `value={selectedIndex !== undefined ? String(selectedIndex) : undefined}` and `onValueChange={(val) => answerQuizQuestion(question.id, Number(val))}`. Each option is a full-width clickable `<Label htmlFor>` wrapping a `RadioGroupItem` + the option text. The selected option gets `border-primary bg-primary/5 ring-2 ring-primary/20` plus a `Check` glyph on the trailing edge; unselected options stay `border-input bg-card` with a `hover:border-primary/40 hover:bg-accent/40` affordance. The Card has `overflow-hidden` so the ring doesn't bleed past the corners.
  - `<CardFooter>` (after a `Separator`): "Back" button (`t("onboarding.back")`) — `variant="outline"`, disabled when `current === 0`, calls `prevQuizQuestion`. On non-final questions, "Next" button (`t("quiz.next")`) — disabled until this question is answered (`quizState.answers[question.id] !== undefined`), calls `nextQuizQuestion`. On the LAST question the button flips to "Submit" (`t("quiz.submit")`) with a `Check` icon, disabled until answered, and on click calls `submitQuiz(total, correctCount, quiz.passingScore)` where `correctCount` is recomputed live via `useMemo` (`questions.filter(q => answers[q.id] === q.correctIndex).length`). The store's `submitQuiz` then sets `submitted=true`, computes the score, and (if passed) issues the certificate + marks all lessons complete + calls `fireConfetti()` — so the confetti burst fires synchronously at submit time and the existing `<ConfettiOverlay/>` (subscribed to `confettiToken`) plays it.
  - Forward/back arrow icons flip with `isRTL` (`NextIcon = isRTL ? ArrowLeft : ArrowRight`, mirrored for Back) so "forward" visually points the right way in Arabic.
- **Results phase (`ResultsView`).** Shown when `quizState.submitted === true`. Wrapped in `<div role="status" aria-live="polite" className="slide-enter">` so screen readers announce the outcome as soon as it mounts. The card border is tinted (`border-success/30` on pass, `border-destructive/30` on fail). Inside:
  - A circular 16×16 icon chip (`bg-success/15 text-success` for pass with `CheckCircle2`, `bg-destructive/15 text-destructive` for fail with `XCircle`).
  - `CardTitle` = `t("quiz.passed")` ("Passed!") or `t("quiz.failed")` ("Not quite — try again"). On pass, a gold `Badge` (`bg-gold text-black`) with `Sparkles` + `t("quiz.confetti")` ("Nailed it!"/"ضبطتها!") sits below the title.
  - Score block: muted `t("quiz.score")` label, then the score as a `text-5xl sm:text-6xl tabular-nums` number (success green on pass, destructive red on fail), a sub-line `ofTotalLine(correctCount, total, locale)` + middot + passing-score reminder, and a `<Progress value={score}>` bar tinted to match the outcome.
  - On pass: a success-tinted inline row with an `Award` icon + the localized `certEarned` string ("You earned a certificate!" / "حصلت على شهادة!"). Footer = single `size="lg"` `t("quiz.backToCourse")` button → `navigate("course-player", { courseId })`.
  - On fail: a centered muted `failHint` line. Footer = two `size="lg"` buttons: `t("quiz.review")` (outline, `ListChecks` icon, toggles `showReview`, carries `aria-expanded`) and `t("quiz.retry")` (default, `RotateCcw` icon) which calls `setShowReview(false)` then `resetQuiz()` — store returns to `current=0`, `answers={}`, `submitted=false`, so the UI flips back to `AnsweringView` for a fresh attempt. When `showReview` is true, the `<ReviewList>` mounts below with the `slide-enter` animation.
- **Review list (`ReviewList`).** Renders every `quiz.questions[i]` in its own Card. Each card has: a numbered circle badge (green when correct, red when wrong), the question text as `CardTitle`, a "Your answer" block (green/red tinted border + bg, `Check`/`XCircle` glyph, shows the user's pick or "—" if unanswered), a "Correct answer" block (green-tinted, shown only when the user got it wrong), and an "Explanation" block (muted bg) showing the localized explanation (`explanationAr ?? explanation`). Labels (`yourAnswer`, `correctAnswer`) and the explanation heading (`t("quiz.explanation")`) all flow through `t()`/`pick()` for proper Arabic.
- **RTL/i18n.** Every visible string + every aria-label goes through `t()` or `pick(locale, STR.x)`. Layout uses only logical CSS (`ms-`, `me-`, `start-`, `end-`, `text-start` — though this screen mostly uses flex/centered layouts, the option `Check` glyph uses `ms-auto` so it lands on the trailing edge in both LTR and RTL). Arrow icons flip with `isRTL`. `<html dir>` is synced by the existing `DirSync` component, so the `.slide-enter` RTL keyframe variant kicks in automatically. Saudi vs Egyptian tones both flow through `t()` (e.g. `quiz.failed` is "ما ضبطت — حاول مرة ثانية" in SA and "مش تمام — حاول تاني" in EG per the existing dict).
- **Accessibility.** The answering Card has `aria-label={questionCounter}` so AT users hear "Question 2 of 5" when the card gains focus. The RadioGroup carries `aria-label={localized.text}` (Radix applies it to the `radiogroup` role). Each option's `Label` is properly associated to its `RadioGroupItem` via `htmlFor`/`id`. Results are wrapped in `role="status" aria-live="polite"` so the pass/fail outcome is announced. The Review toggle button exposes `aria-expanded`. The no-quiz `EmptyState` keeps its default semantics. Color is never the only signal — selected state also gets a `Check` glyph + violet ring; pass/fail also gets distinct iconography + score color; correct/incorrect in review also gets `Check`/`XCircle` glyphs.
- **`react-hooks/set-state-in-effect` rule.** Satisfied by construction: the only `useEffect` calls `startQuiz` (a Zustand action, not a React setState). The `showReview` toggle and the Retry's `setShowReview(false)` are both fired from event handlers, not effects. `useMemo` is used for `correctCount` (no setState involved). No `useState` setter is ever called synchronously inside an effect body.
- Lint: `cd /home/z/my-project && bun run lint` → my file (`src/components/screens/QuizScreen.tsx`) is CLEAN — `bunx eslint src/components/screens/QuizScreen.tsx` produces zero output. The full repo `bun run lint` currently reports 1 error in `src/components/screens/CheckoutScreen.tsx` (`react-hooks/refs`: "Cannot update ref during render" at line 103) — that is another agent's WIP file, not in scope for Task 3-i and not modified by me. `bunx tsc --noEmit` reports zero errors originating from `QuizScreen.tsx` (remaining tsc errors are all in `examples/`, `skills/`, and `src/components/layout/Header.tsx` — pre-existing and out of scope).

Stage Summary:
- `QuizScreen` is a NAMED export at `src/components/screens/QuizScreen.tsx`, ready for the root `page.tsx` (Task ID 4) to render when `route.name === "quiz"`.
- No foundation files were modified. No new shared components were added. One new file created.
- Honors all spec requirements: SPA at `/` (no new Next.js routes), `'use client'` at top, reads `courseId` from `route.params`, looks up quiz via `getQuizForCourse`, EmptyState + back-to-course when no quiz, auto-`startQuiz` on mount with infinite-loop guard, StepProgressBar + "Question X of Y", one-question-per-screen Card with RadioGroup of selectable option cards (violet ring + check glyph on selected), Back/Next/Submit footer with disabled-until-answered and Submit-on-last-question that computes `correctCount` and calls `submitQuiz(total, correctCount, passingScore)`, `.slide-enter` keyed by `current` for per-question slide transitions, results phase with `role="status"` announcing pass (green check + score + certificate line + back-to-course) or fail (red X + score + Retry + Review toggle), Review list with per-question your-answer/correct-answer/explanation blocks tinted green/red, prominent score in both phases, confetti fires automatically via `submitQuiz` on pass, full RTL + Saudi/Egyptian Arabic via logical CSS and `t()`/inline bilingual helpers, and the `react-hooks/set-state-in-effect` rule satisfied throughout.

---
Task ID: 3-h
Agent: sub-agent (Cart → Checkout → Confirmation flow)
Task: Build the Cart, Checkout, and Confirmation screens (Udemy-inspired e-commerce flow)

Work Log:
- Read the worklog + foundation files first: `src/lib/store.ts` (cart slice: `cart` as `CartItem[]` of `{courseId, addedAt}`, `removeFromCart`, `clearCart`, `isInCart`; enrollments `enrolledCourseIds` + `enroll(courseId)`; `getCourseProgress(courseId)` returns 0..100; `certificates` + `issueCertificate(courseId, score?)`; routing `navigate(name, params?)`, `route.params` for the confirmation's `orderId`/`amount`; `pushToast({title, variant})`; `fireConfetti()`; `useAppStore.getState()` for non-reactive reads inside async callbacks), `src/lib/types.ts` (`CartItem`, `Route`, `RouteName` includes `cart | checkout | checkout-confirmation`, `Course`), `src/hooks/use-i18n.ts` (`useI18n()` → `{ t, locale, dialect, dir, isRTL, formatNumber, formatPrice, … }`), `src/lib/i18n.ts` (existing keys: `cart.title`/`cart.empty`/`cart.emptySub`/`cart.browse`/`cart.subtotal`/`cart.total`/`cart.promo`/`cart.promoApply`/`cart.promoApplied`({code})/`cart.promoInvalid`/`cart.remove`/`cart.checkout`/`cart.continueShopping`/`cart.itemCount`({n}), `checkout.title`/`checkout.subtitle`/`checkout.cardName`/`checkout.cardNumber`/`checkout.expiry`/`checkout.cvc`/`checkout.pay`({amount})/`checkout.back`/`checkout.secure`, `confirmation.title`/`confirmation.subtitle`/`confirmation.orderId`({id})/`confirmation.amount`/`confirmation.cta`/`confirmation.confetti`, plus `card.hours`({n}) — all present in EN / arSa / arEg), `src/lib/mock-data.ts` (`getCourse`, `getInstructor`, `promoCodes` = `{ EDIFY10: 0.1, WELCOME25: 0.25, MVP50: 0.5 }`), `src/components/shared/{CourseThumb,EmptyState,StepProgressBar}.tsx`, `src/components/ui/{button,card,input,label,separator}.tsx`, `src/components/shared/ConfettiOverlay.tsx` (subscribes to `confettiToken`), and the existing `SearchScreen`/`OnboardingScreen` patterns for StepProgressBar usage, RTL icon-flipping, and the `react-hooks/set-state-in-effect` workaround.
- Created THREE new files (no foundation files modified, no new shared components needed):
  1. `src/components/screens/CartScreen.tsx` — NAMED export `CartScreen`, `"use client"` at top.
  2. `src/components/screens/CheckoutScreen.tsx` — NAMED export `CheckoutScreen`, `"use client"` at top.
  3. `src/components/screens/CheckoutConfirmationScreen.tsx` — NAMED export `CheckoutConfirmationScreen`, `"use client"` at top.
- CartScreen layout: `max-w-6xl` container, header row with `cart.title` + `cart.itemCount`({n}) on the start side and a ghost `cart.continueShopping` button on the end side. Two-column grid `lg:grid-cols-[1fr_360px]` with cart items on the left and a sticky (`lg:sticky lg:top-20`) order summary Card on the right. Empty-cart guard: when `cart.length === 0`, returns a centered Card wrapping `EmptyState` (icon=ShoppingCart, `cart.empty`, `cart.emptySub`) + a primary `cart.browse` button → `navigate("search")`.
- CartScreen item card: a `Card` per cart row with `flex gap-3 sm:gap-4`. LEFT = `CourseThumb` (w-28 on mobile, w-40 on desktop) wrapped in a button → `navigate("course-landing", { courseId })` with `aria-label={title}`. MIDDLE = title (locale-aware, line-clamp-2, hover:text-primary, also a button to course-landing), instructor name (via `getInstructor(course.instructorId)`), level + `card.hours`({n}) muted meta row, and a price row with `formatPrice(course.price)` bold + `formatPrice(course.originalPrice)` strikethrough when present. RIGHT = a ghost "Remove" button (`Trash2` icon, `cart.remove` text hidden on mobile, `aria-label` includes the title) that calls `removeFromCart(courseId)` + `pushToast({title: "${title} — Removed from cart"})` (locale-aware inline label since the i18n file is protected).
- CartScreen order summary: a Card with `cart.total` header. Promo form: `Label` + `Input` (uppercase, maxLength 20, placeholder "EDIFY10", `autoCapitalize="characters"`) + secondary "Apply" button. On submit: trims + uppercases the input, looks it up in `promoCodes`; if valid, `setAppliedPromo({code, discountFraction})` + `pushToast({title: cart.promoApplied({code}), variant: "success"})`; if invalid, `setAppliedPromo(null)` + `pushToast({title: cart.promoInvalid, variant: "destructive"})`. When applied, a green `bg-success/10 border-success/30` chip shows `CheckCircle2` + `cart.promoApplied`({code}) + an X button to remove the promo. A small muted hint "Try: EDIFY10 · WELCOME25 · MVP50" (AR-translated) helps demo discovery. Totals section: `cart.subtotal` row (strikethrough when promo applied), `appliedPromo.code (NN%)` + `−formatPrice(discountAmount)` row in success green when promo applied, then `cart.total` label + `formatPrice(total)` (xl bold). `total = subtotal * (1 - discountFraction)`. Primary `cart.checkout` button (size lg, full width, RTL-aware `ArrowRight`/`ArrowLeft` forward icon) → `navigate("checkout")`. Secondary ghost `cart.continueShopping` button below.
- CheckoutScreen layout: `max-w-4xl` container. Top = `StepProgressBar current={1} total={3} labels={stepLabels}` wrapped in a `role="progressbar" aria-valuenow={2} aria-valuemin={1} aria-valuemax={3} aria-label={checkout.title}` div (the shared component already exposes a sr-only percentage; the wrapper adds the explicit aria-valuenow the spec asked for). `stepLabels` is inline bilingual `["Cart","Payment","Done"]` (EN) / `["السلة","الدفع","تم"]` (AR) — kept inline because `i18n.ts` is a protected foundation file (same pattern OnboardingScreen used). Header: `checkout.title` (h1) + `checkout.subtitle` (muted) + a link-styled `checkout.back` button → `navigate("cart")` with an RTL-flipped `ArrowLeft`/`ArrowRight` back icon. Secure-notice banner: rounded-md `bg-primary/5 border-primary/20` with `Lock` icon + `checkout.secure`. Two-column grid `lg:grid-cols-[1fr_320px]`: payment form Card on the left, sticky order-summary Card on the right.
- CheckoutScreen payment form: four fields with `Label` + `Input` + inline `role="alert"` error paragraphs linked via `aria-describedby` + `aria-invalid`:
  - Name on card (`checkout.cardName`): `autoComplete="cc-name"`, error if empty.
  - Card number (`checkout.cardNumber`): `formatCardNumber()` strips non-digits, caps at 16 digits, inserts a space every 4 digits (maxLength=19 to fit "0000 0000 0000 0000"), `inputMode="numeric"`, `autoComplete="cc-number"`, error if empty or < 12 digits after stripping spaces.
  - Expiry (`checkout.expiry`): `formatExpiry()` strips non-digits, caps at 4 digits, inserts "/" after the first two (maxLength=5), `autoComplete="cc-exp"`, placeholder "MM/YY", error if empty / not `^\d{2}\/\d{2}$` / month < 1 or > 12.
  - CVC (`checkout.cvc`): strips non-digits, caps at 4 (maxLength=4), `autoComplete="cc-csc"`, placeholder "123", error if empty or not `^\d{3,4}$`.
  Errors are hidden until a field is blurred (`touched[field]=true` on `onBlur`) or the Pay button is clicked (which sets all `touched` to true via `handlePay`). Errors use `t("checkout.<field>")` as the base label plus a short suffix (e.g. "— 12+", "— MM/YY", "— 3-4").
- CheckoutScreen order summary: Card listing each cart course (locale-aware title line-clamp-2 + `formatPrice(course.price)`), then `Separator` + `cart.subtotal` row, `Separator` + `cart.total` row with `formatPrice(total)`. Per spec, `total = subtotal` at checkout (no promo re-applied here — the cart's local promo state is lost on navigation, and the spec said to keep it simple). Pay button (size lg, full width): when idle shows `Lock` + `checkout.pay`({amount: formatPrice(total)}) + RTL-flipped forward icon; when `processing` shows `Loader2 animate-spin` + "Processing…" (AR: "جارٍ المعالجة…"). `disabled={processing}` + `aria-busy={processing}`. Empty-cart guard: if `cartRows.length === 0 && !processing`, renders a compact Card with a ShoppingCart icon, `cart.empty`, and two buttons (`checkout.back` → cart, `cart.browse` → search).
- CheckoutScreen pay flow: `handlePay` validates (`setTouched` all true, bail if `!isValid || processing || cartRows.length === 0`), then `setProcessing(true)` + `setPayToken(n => n+1)`. A `useEffect` keyed on `payToken` short-circuits when `payToken === 0`, otherwise schedules `window.setTimeout(callback, 1200)` with a `clearTimeout` cleanup. **setState is only called inside the timeout callback (and inside the click handler) — never synchronously in the effect body** — so `react-hooks/set-state-in-effect` stays happy. The callback: reads `useAppStore.getState()` for fresh state, iterates `state.cart`, calls `state.enroll(item.courseId)` for each, and if `state.getCourseProgress(item.courseId) >= 100` calls `state.issueCertificate(item.courseId)` (handles the re-purchase-of-finished-course demo edge case). Then `generateOrderId()` (`Math.random().toString(36).slice(2,8).toUpperCase()`), `state.clearCart()`, `state.navigate("checkout-confirmation", { orderId, amount: String(total) })`, `setProcessing(false)`. The closure captures `total` as a primitive at the moment payToken flips — stable because the button is disabled while processing.
- CheckoutConfirmationScreen layout: `max-w-2xl` centered container with a single Card. On mount: a `useEffect` calls `useAppStore.getState().fireConfetti()` — `fireConfetti` is a Zustand action (not a React setState), so calling it inside useEffect is safe and does not trip the rule. Card content: big success check (`CheckCircle2` in a `bg-success/15` 16×16 circle, success-green) + `confirmation.title` + `confirmation.subtitle` + a small gold `confirmation.confetti` banner (`Sparkles` + `bg-gold/15` pill). Order details box: a `bg-muted/30 border-border rounded-md p-4` flex column with `confirmation.orderId`({id}) as a centered mono header line, a `Separator`, then a `confirmation.amount` row with `formatPrice(Number(route.params.amount))` bold. "Now in your learning" section (inline bilingual label "Now in your learning" / "كورساتك الآن في تعلّمي"): a `<ul>` of the 6 most-recent enrolled courses (read from `enrolledCourseIds` → `getCourse` → filter Boolean → `.slice(-6).reverse()` so the freshly purchased ones sit at the top), each a button row → `navigate("course-landing", { courseId })` with a w-20 `CourseThumb` + line-clamp-2 title + `aria-label`. CTAs: primary `confirmation.cta` ("Start learning") button (size lg, `GraduationCap` icon, RTL-flipped forward arrow) → `navigate("my-learning")`; secondary outline button "Browse more courses" / "تصفّح كورسات أكتر" (`Search` icon) → `navigate("search")`.
- RTL/i18n: every visible string + every aria-label goes through `t()` or inline bilingual labels. Layout uses only logical CSS (`ms-`, `me-`, `ps-`, `pe-`, `start-`, `end-`, `text-start`) so icon positions, sticky sidebars, and arrow directions flip automatically in Arabic. Forward/back arrow icons flip with `isRTL` (`ForwardIcon = isRTL ? ArrowLeft : ArrowRight`, `BackIcon = isRTL ? ArrowRight : ArrowLeft`). `<html dir>` is synced by the existing `DirSync` component. Saudi vs Egyptian tones flow through the same `t()` (e.g. `checkout.secure` and `confirmation.title` resolve to dialect-specific strings).
- Accessibility: forms have associated `<Label htmlFor>` + `aria-describedby` + `aria-invalid`; error paragraphs use `role="alert"`; the step progress wrapper exposes `role="progressbar"` with `aria-valuenow`/`aria-valuemin`/`aria-valuemax`/`aria-label`; the Pay button uses `aria-busy={processing}`; cart item remove buttons include the course title in their `aria-label`; the confirmation's enrolled-course list items are real `<button>`s with `aria-label={title}`; color is never the only signal (success states pair the green check icon with the success color, error states pair the destructive text with `role="alert"`).
- Lint: `cd /home/z/my-project && bun run lint` → CLEAN (exit 0, no errors, no warnings). First pass flagged one `react-hooks/refs` error in CheckoutScreen ("Cannot update ref during render") because the initial implementation stored `total` in a `useRef` and assigned `totalRef.current = total` during render; fixed by removing the ref entirely and capturing `total` directly in the effect closure (safe because the Pay button is disabled during processing so the cart/total can't change mid-wait). `bunx tsc --noEmit` reports zero errors originating from any of the three new files.

Stage Summary:
- Three NAMED exports ready for the root `page.tsx` (Task ID 4) to render based on `route.name`:
  - `CartScreen` when `route.name === "cart"`
  - `CheckoutScreen` when `route.name === "checkout"`
  - `CheckoutConfirmationScreen` when `route.name === "checkout-confirmation"`
- No foundation files modified. No new shared components added. Three new files created under `src/components/screens/`.
- Honors all spec requirements: SPA at `/` (no new Next.js routes), `"use client"` at top of every file, Cart two-column with sticky promo summary + Apply/Remove promo + strikethrough totals, Checkout 3-step progress + secure banner + 4-field payment form with inline validation + processing spinner + enroll-each-cart-item + certificate-when-100% + clearCart + navigate-to-confirmation, Confirmation fires confetti on mount + success check + order id/amount + "Now in your learning" list + Start-learning/Browse-more CTAs, full RTL + Saudi/Egyptian Arabic via logical CSS and `t()`/inline bilingual helpers, and the `react-hooks/set-state-in-effect` rule satisfied throughout (checkout processing delay scheduled via `setTimeout` inside `useEffect` with setState only in the timeout callback; confirmation confetti uses `useAppStore.getState().fireConfetti()` which is not a setState).

---
Task ID: 3-e
Agent: sub-agent (CourseLandingScreen)
Task: Build the Course Landing screen (Udemy + Coursera merged course-detail page)

Work Log:
- Read the worklog + foundation files first: `src/lib/store.ts` (course landing uses `route.params?.courseId`, `navigate("course-player"|"checkout"|"cart"|"search"|"course-landing", {courseId})`, `addToCart/removeFromCart/isInCart`, `enroll/isEnrolled`, `toggleSaved/isSaved`, `pushToast({title, variant})`, `goBack`, and `getCourseProgress` not needed here), `src/lib/types.ts` (`Course` with `sections[].lessons[]` typed `Lesson.type: "video"|"reading"|"quiz"`, `reviews[]`, `qa[]`, `faqs[]`, `includes{hoursOfVideo,articles,downloadableResources,mobileAccess,certificate}`, `Instructor` with `avatar` URL + `rating/students/coursesCount`, `CourseLevel = Beginner|Intermediate|Advanced`), `src/hooks/use-i18n.ts` (`useI18n()` → `{ t, locale, dialect, dir, isRTL, formatNumber, formatPrice, formatDate, formatDuration }`), `src/lib/i18n.ts` (existing keys `landing.skillsTitle/syllabus/syllabusSub/instructor/includes/includes.videos/articles/resources/mobile/certificate/reviews/reviewsSub/faq/related/enroll/addToCart/buyNow/goToCourse/alreadyEnrolled/lessons/min/preview/hours/updated/allLevels`, `card.bestseller`, `card.new`, `card.hours`, `card.lessons`, `common.off` ({n}), `onboarding.back`), `src/lib/mock-data.ts` (`getCourse(id)`, `getInstructor(id)`, `courses` for related list), `src/components/shared/StarRating.tsx` (`<StarRating value size showValue count />` — gold partial-fill overlay), `src/components/shared/CourseThumb.tsx` (gradient 16:9 placeholder), `src/components/shared/CourseCard.tsx` (`variant="compact"` for related), `src/components/shared/EmptyState.tsx`, and shadcn UI primitives `button/card/badge/separator/avatar/accordion`. The Accordion's default `AccordionTrigger` adds a `ChevronDownIcon` that rotates 180° on open and ships with `text-left` (physical) — I override with `text-start` on every trigger to make it RTL-correct, and use `type="multiple"` (with the first section open by default) for the syllabus and `type="single" collapsible` for the FAQ.
- Added NEW i18n keys to ALL THREE dictionaries (en / arSa / arEg) in `src/lib/i18n.ts`, inserted right after the existing `landing.allLevels` line in each dict (so existing key/value pairs are untouched — only new keys appended): `landing.notFound`, `landing.notFoundDesc`, `landing.backToBrowse`, `landing.showMore`, `landing.showLess`, `landing.wishlist`, `landing.saved`, `landing.removed`, `landing.instructorStudents` ({n}), `landing.instructorCourses` ({n}), `landing.instructorRating`, `landing.alreadyInCart`, `landing.addedToCart`, `landing.removedFromCart`, `landing.enrolledSuccess`, `landing.relatedEmpty`, `landing.ratingBreakdown`, `landing.lessonsCount` ({n}), `landing.sectionCount` ({n}), `landing.shareCourse`, `landing.shareToast`. The Saudi and Egyptian tones diverge where appropriate (e.g. `landing.notFoundDesc` = "ماقدرنا نلاقي الكورس اللي تدوّر عليه. يمكن اتغير مكانه أو اتشال." (SA) vs "مقدرناش نلاقي الكورس اللي بتدوّر عليه. ممكن يكون اتغيّر مكانه أو اتشال." (EG); `landing.enrolledSuccess` = "تم اشتراكك! يلا نبدأ التعلم." (SA) vs "اتسجلت! يلا نبدأ نتعلّم." (EG); `landing.showMore` = "اعرض المزيد من التقييمات" (SA) vs "عرض كمان تقييمات" (EG)). Two helper constants are kept inline (not in i18n.ts) because that file is protected: `LEVEL_AR = { Beginner: "مبتدئ", Intermediate: "متوسط", Advanced: "متقدم" }` and `RATING_BREAKDOWN = [{5,70},{4,20},{3,6},{2,2},{1,2}]` (mock distribution per the spec).
- Created `/home/z/my-project/src/components/screens/CourseLandingScreen.tsx` — NAMED export `CourseLandingScreen`, `'use client'` at top. Single file with private sub-components co-located for clarity (`CourseNotFound`, `CourseLandingContent`, `HeaderBlock`, `Dot`, `SkillsSection`, `SyllabusSection`, `LessonRow`, `InstructorSection`, `ReviewsSection`, `FaqSection`, `RelatedSection`, `PricingCard`, `MobileCTABar`). No new shared files needed.
- Top-level component reads `useAppStore((s) => s.route.params?.courseId)`, looks up the course via `getCourse(id)`. If the course is missing or `courseId` is undefined → renders `<CourseNotFound/>` which is a centered `EmptyState` with `BookX` icon, `landing.notFound` title, `landing.notFoundDesc` description, and two buttons (ghost `onboarding.back` → `goBack()`, primary `landing.backToBrowse` → `navigate("search")`). Otherwise renders `<CourseLandingContent course={course} />` so the rest of the tree can assume `course` is non-null (the protected-foundation `EmptyState` import handles the visual).
- Layout: `max-w-7xl mx-auto px-4 sm:px-6 py-6 lg:py-8` container with extra bottom padding on mobile (`pb-28 lg:pb-8`) to make room for the sticky bottom CTA bar. Inside: `lg:grid lg:grid-cols-[minmax(0,1fr)_360px] lg:gap-8 xl:gap-10` two-column layout. MAIN column is a `space-y-10 lg:space-y-12` flex column with all the content sections. RIGHT RAIL (`hidden lg:block`) wraps a `sticky top-20` PricingCard. The MobileCTABar is `lg:hidden fixed inset-x-0 bottom-0 z-40 bg-card/95 backdrop-blur border-t` and renders below the grid (always in the DOM but visually pinned to the bottom of the viewport on mobile only).
- MAIN column sections (in order):
  1. **Header** (`HeaderBlock`): `<section aria-label={title}>` with a top row of `Badge`s — secondary category chip (locale-aware `course.categoryAr`/`course.category`), gold `card.bestseller` when `course.bestseller`, outline-primary `card.new` when `course.isNew`. Then `<h1>` (text-2xl → lg:text-4xl bold leading-tight) with the locale-aware title, `<p>` subtitle. Then a meta row of inline items separated by a small `Dot` (`·`, aria-hidden): `StarRating value={course.rating} size={16} showValue count={course.ratingCount}`, enrolled count with `Users` icon + `t("landing.instructorStudents", { n: formatNumber(course.enrolledCount) })` (the label is just "{n} students" so it doubles for course-level and instructor-level counts), level with `GraduationCap` icon + `LEVEL_AR[course.level]` (locale-aware), language with `Globe` icon + `course.language`, last-updated with `Clock` icon + `t("landing.updated", { date: formatDate(course.lastUpdated) })`. Below the meta row, an "Instructor: <name>" line where the name is a real `<a href="#instructor">` link that jumps to the instructor section (which has `id="instructor"` + `scroll-mt-20` for sticky-header offset).
  2. **Mobile inline PricingCard** (only rendered inside `<div className="lg:hidden">` so it appears in the main column on mobile and never duplicates with the desktop rail).
  3. **Skills** (`SkillsSection`): `<section aria-labelledby="skills-heading">` with `landing.skillsTitle` heading + a `flex flex-wrap gap-2` of pill `Badge`s (`variant="outline"` + `bg-primary/10 text-primary border-primary/20`, `rounded-full`, `px-3 py-1.5`). Locale-aware `skillsAr`/`skills`.
  4. **Syllabus** (`SyllabusSection` + `LessonRow`): `<section aria-labelledby="syllabus-heading">` with `landing.syllabus` heading + `landing.syllabusSub` ({sections}/{lessons}/{hours}) sub. A `Card` (p-0 gap-0 overflow-hidden) wraps an `Accordion type="multiple" defaultValue={[course.sections[0]?.id]}` so the first section is open on mount. Each `AccordionItem` has a custom trigger: a two-line block (section title font-semibold line-clamp-2 + `{n} lessons • {total} min` meta) with `text-start` to override the foundation's `text-left` and `hover:no-underline` to disable the default underline-on-hover. The chevron is the foundation's built-in one (rotates 180° on open via `[&[data-state=open]>svg]:rotate-180`). `AccordionContent` renders a `<ul className="divide-y divide-border/50">` of `LessonRow` items. Each `LessonRow` is a `<li>` with `flex items-center gap-3 py-2.5 px-2 rounded-md hover:bg-muted/60 transition-colors`, the lesson-type icon (`PlayCircle` for video, `BookOpen` for reading, `FileQuestion` for quiz — picked via a ternary and assigned to a typed `LucideIcon`), locale-aware title (`truncate`), and `formatDuration(lesson.durationMin)` on the end (`tabular-nums`).
  5. **Instructor** (`InstructorSection`): `<section id="instructor" aria-labelledby="instructor-heading" className="scroll-mt-20">` with `landing.instructor` heading. A `Card p-5 sm:p-6 gap-4` containing a `flex items-start gap-4` row: shadcn `Avatar w-16 h-16 border` with `AvatarImage src={instructor.avatar} alt={name}` and an `AvatarFallback` (locale-aware initial, primary-tinted background) for the SSR/no-image case. Right side: `<h3>` name (locale-aware `nameAr ?? name`), muted title (locale-aware), and a meta row with `Star`+rating+`landing.instructorRating`, `Users`+`landing.instructorStudents` ({n: formatNumber(instructor.students)}), and `GraduationCap`+`landing.instructorCourses` ({n}). Below the row: a paragraph with the locale-aware bio (`bioAr ?? bio`).
  6. **Reviews** (`ReviewsSection`): `<section aria-labelledby="reviews-heading">` with `landing.reviews` heading + `landing.reviewsSub` ({n: formatNumber(course.ratingCount)}). A `grid sm:grid-cols-[200px_minmax(0,1fr)] gap-5`: LEFT = a `Card p-5 gap-3 h-fit` with centered big rating number (`text-4xl font-bold tabular-nums`), `StarRating` centered, muted `landing.reviewsSub` count, a `Separator`, then a stack of 5 rating-breakdown rows (`aria-label={landing.ratingBreakdown}`) — each row is `flex items-center gap-2 text-xs` with the star number, a `Star` icon (gold, `fill="currentColor" strokeWidth={0}`), a custom bar (`flex-1 h-2 bg-muted rounded-full overflow-hidden min-w-[60px]` containing a `h-full bg-gold/80 rounded-full` div whose `width` is set inline to `${pct}%`), and a `${pct}%` label. Using inline width instead of shadcn `Progress` because the latter animates with `translateX(-X%)` which doesn't respect RTL; the inline-width div fills from the start edge in both LTR and RTL because it inherits `<html dir>`. RIGHT = a `<ul className="space-y-5">` of reviews (3 by default, all when expanded). Each review is a `<li>` with a 9×9 violet-tinted circle holding the student's initial (`aria-hidden`), the student name (font-semibold), `StarRating size={12}` + `formatDate(review.date)`, and the locale-aware comment (`commentAr ?? comment`). A `Button variant="outline" mt-5` toggles `showAll` via `setShowAll((v) => !v)` and exposes `aria-expanded={showAll}`; its label flips between `landing.showMore` and `landing.showLess`.
  7. **FAQ** (`FaqSection`): `<section aria-labelledby="faq-heading">` with `landing.faq` heading + an `Accordion type="single" collapsible` wrapped in `bg-card border rounded-xl px-4`. Each `AccordionItem` uses `text-start text-sm sm:text-base font-semibold hover:no-underline` trigger with the locale-aware question (`questionAr ?? question`) and a muted `text-sm text-muted-foreground leading-relaxed` content with the locale-aware answer (`answerAr ?? answer`). Last item gets `border-b-0` to avoid a trailing divider.
  8. **Related** (`RelatedSection`): `<section aria-labelledby="related-heading">` with `landing.related` heading. `useMemo` filters `courses` to same category, excludes the current course, slices to 3. Renders a `Card p-2 sm:p-3 gap-0` containing a `<ul className="divide-y divide-border/60">` of `<li>` wrappers around `<CourseCard course={c} variant="compact" />` (the shared compact variant already wires navigation to `course-landing` with the new courseId, locale-aware title/instructor/price, mini thumb, and rating). Empty list → section is skipped entirely.
- RIGHT RAIL (`PricingCard`, also used for the mobile inline card): a `Card overflow-hidden p-0 gap-0 shadow-md` containing:
  - `CourseThumb` at the top (gradient 16:9 placeholder + level chip).
  - A `p-5 space-y-4` body: price row (`text-3xl font-bold tabular-nums` price + strikethrough `originalPrice` + gold `Badge bg-gold text-black hover:bg-gold` showing `common.off` ({n: discountPct}) when there's a discount; `discountPct` is computed inline as `Math.round(((originalPrice - price) / originalPrice) * 100)`).
  - CTA stack: **If enrolled** → a full-width success Badge (`bg-success text-success-foreground gap-1 w-full justify-center py-1.5` with `Check` icon + `landing.alreadyEnrolled`) followed by a primary `Button size="lg w-full"` with `PlayCircle` icon + `landing.goToCourse` → `onGoToCourse` (navigates to course-player). **If not enrolled** → primary `Button size="lg w-full"` (`GraduationCap` + `landing.enroll`) → `onEnroll` (calls `enroll(courseId)` + `pushToast` success + `navigate("course-player")`); a row with a `Button variant="secondary flex-1"` (cart toggle: `Check`+`landing.alreadyInCart` when in cart, `ShoppingCart`+`landing.addToCart` otherwise) and a `Button variant="ghost size="icon" border` for the wishlist heart (`Heart` with `fill-rose-500 text-rose-500` when saved, `aria-pressed={isSaved}`, `aria-label={landing.wishlist}`); and an outline `Button w-full` `landing.buyNow` → `onBuyNow` (addToCart if not in cart + `navigate("checkout")`).
  - `Separator`.
  - **Includes list** (`landing.includes` heading + `<ul className="space-y-2 text-sm">`): `PlayCircle` + `landing.includes.videos` ({n: hoursOfVideo}); `BookOpen` + `landing.includes.articles` ({n}); `Download` + `landing.includes.resources` ({n}); mobile-access row with `Check` (success) or `X` (muted) + `Smartphone` inline icon + `landing.includes.mobile` (line-through when not available); certificate row with `Check`/`X` + `Award` inline icon + `landing.includes.certificate` (same line-through treatment).
- All event handlers (`handleEnroll`, `handleGoToCourse`, `handleBuyNow`, `handleCartToggle`, `handleWishlist`) live in `CourseLandingContent` and are passed down to both the rail `PricingCard` and the `MobileCTABar` via a single `pricingActions` object spread. Each handler fires the relevant store action(s) and a `pushToast` with the locale-aware success/info title. The cart and wishlist handlers toggle correctly (removeFromCart when already in cart, "Removed" toast when un-saving, etc.).
- Mobile sticky bottom bar (`MobileCTABar`): `lg:hidden fixed inset-x-0 bottom-0 z-40 bg-card/95 backdrop-blur border-t border-border shadow-[0_-4px_12px_rgba(0,0,0,0.06)]` with `role="region" aria-label={landing.enroll}`. Inner row `flex items-center gap-2`: compact price block (bold tabular-nums price + tiny strikethrough originalPrice), `flex-1` spacer, ghost icon `Button` for the wishlist heart (`aria-pressed`, `aria-label={landing.wishlist}`, `border`), conditional cart toggle button (hidden when enrolled; icon-only on `<sm`, icon+label on `sm+`), and a primary `Button` that's either `PlayCircle` + `landing.goToCourse` (enrolled) or `GraduationCap` + `landing.enroll` (not enrolled). All `shrink-0` so the price text never wraps over them.
- RTL/i18n: every visible string and every aria-label goes through `t()`. Layout uses only logical CSS (`ps-`, `pe-`, `ms-`, `me-`, `start-`, `end-`, `text-start`, `gap-x-*`) so the heart/cart buttons on the rail, the price block on the mobile bar, the rating-breakdown bar widths, and the avatar/meta rows all flip correctly in Arabic. The instructor name link uses `<a href="#instructor">` which respects RTL scrolling. The Accordion's built-in chevron sits at the end of the flex container (flips automatically with `dir`) and the trigger text uses `text-start` to override the foundation's `text-left`. `<html dir>` is synced by the existing `DirSync` component so no manual dir handling is needed inside the screen.
- Accessibility: each section is a `<section aria-labelledby>` with a real `<h2 id="…-heading">`. The H1 is the course title. The syllabus `AccordionTrigger` is a real `<button>` (Radix provides `aria-expanded`/`aria-controls` automatically); the FAQ uses the same Radix accordion. The reviews list is a real `<ul>`/`<li>`. The show-more button exposes `aria-expanded`. The wishlist buttons expose `aria-pressed`. Every icon-only button has a clear `aria-label`. The includes list uses `Check`/`X` icons so the availability state isn't communicated by color alone (and pairs with line-through when false). The instructor avatar has `alt={name}` on `AvatarImage` and a readable `AvatarFallback` with the initial. The mobile CTA bar is a `role="region"` with `aria-label`. Color is never the only signal — saved/wishlisted heart also fills rose, in-cart button also swaps to a Check + "Already in cart" label, etc.
- No `useEffect` is used anywhere in this screen (all state is either via Zustand subscriptions or local `useState` driven purely by event handlers), so the `react-hooks/set-state-in-effect` rule is satisfied by construction. The only `useMemo` is the related-courses filter (keyed on `course.id` + `course.category`).
- Lint: `cd /home/z/my-project && bun run lint` → CLEAN (exit 0, no errors, no warnings). `bunx tsc --noEmit` reports zero errors originating from `CourseLandingScreen.tsx` or the `i18n.ts` edits (the only remaining tsc errors are pre-existing ones in `examples/`, `skills/`, and `src/components/layout/Header.tsx` — out of scope for this task).

Stage Summary:
- `CourseLandingScreen` is a NAMED export at `src/components/screens/CourseLandingScreen.tsx`, ready for the root `page.tsx` (Task ID 4) to render when `route.name === "course-landing"`.
- No protected foundation files modified except `src/lib/i18n.ts`, where new keys were *appended* to all three dictionaries (en / arSa / arEg) without altering any existing key or its value.
- Honors all spec requirements: SPA at `/` (no new Next.js routes), `'use client'` at top, locale-aware title/subtitle/skills/sections/lessons/reviews/FAQs/instructor, 2-column desktop (main + sticky right rail) + single-column mobile with sticky bottom CTA bar, header with category badge + H1 + subtitle + StarRating + enrolled count + level + language + last-updated + instructor link, skills pill chips, accordion syllabus with lesson type icons + durations, separate instructor card with avatar/stats/bio, reviews with 5-bar rating breakdown (mock distribution 70/20/6/2/2) + expandable review list, FAQ accordion, 3 related CourseCards (compact variant) from same category, right-rail PricingCard with thumb + price + discount badge + Enroll/Add-to-cart/Buy-now (or Go-to-course when enrolled) + wishlist heart + "This course includes" list with check/X icons, mobile sticky CTA bar with price + heart + cart toggle + primary CTA, full RTL + Saudi/Egyptian Arabic via logical CSS and `t()`, accessibility landmarks/aria-expanded/aria-pressed/aria-labels, and the `.view-enter` entrance animation.

---
Task ID: 3-f
Agent: sub-agent (CoursePlayerScreen)
Task: Build the Course Player screen — Udemy-inspired immersive in-course experience with simulated video, curriculum sidebar, content tabs, and a mock AI tutor

Work Log:
- Read the worklog + foundation files first: `src/lib/store.ts` (player uses `route.params?.courseId`, `navigate("course-landing"|"quiz"|"home", {courseId})`, `progress` Record<courseId, CourseProgress>, `markLessonComplete`, `setLessonPosition`, `getCourseProgress`, `getCurrentLessonId` = first uncompleted lesson or last lesson if all done, `addNote(courseId, lessonId, text, sec)`, `extraQA` Record<courseId, QAItem[]>, `addQuestion(courseId, question, studentName)`, `pushToast({title, description?, variant?})`, `user` for Q&A author name), `src/lib/types.ts` (`Lesson.type: "video"|"reading"|"quiz"` with `titleAr?/descriptionAr?/transcript/transcriptAr?/resources?: {name, type: "pdf"|"zip"|"slides"}[]`, `CourseProgress.lessons[lessonId].{completed, positionSec, notes[]}`, `QAItem.{id, question, questionAr?, answer, answerAr?, studentName, upvotes, date}`), `src/hooks/use-i18n.ts` (`useI18n()` → `{ t, locale, dialect, dir, isRTL, formatNumber, formatPrice, formatDate, formatDuration }`), `src/lib/i18n.ts` (existing `player.*` keys: `markComplete/completed/nextLesson/prevLesson/curriculum/progress/aiAssistant/aiToggle/aiPlaceholder/aiGreeting/aiThinking/tab.{overview,qa,notes,reviews,resources}/qa.{ask,askPlaceholder,empty,upvotes}/notes.{placeholder({time}),add,empty,at}/resources.{empty,download}/overview.{views({n}),duration({n}),enrolled({n}),language}/autoResumed/takeQuiz`, plus `landing.min` and `quiz.title`), `src/lib/mock-data.ts` (`getCourse(id)`, `getInstructor(id)`, `getAllLessons(course)` returns flat list with `sectionId`, `getLessonById(course, lessonId)` returns `{lesson, section}`, `getQuizForCourse(courseId)` may be undefined), `src/components/shared/StarRating.tsx` (`<StarRating value size showValue count />` gold partial-fill overlay), `src/components/shared/EmptyState.tsx` (`{icon, title, description?, action?, className?}`), `src/hooks/use-mobile.ts` (`useIsMobile()` returns boolean, starts `false` then resolves after mount), and shadcn UI primitives `button/card/badge/separator/tabs/textarea/input/sheet/collapsible/avatar`. Confirmed existing CourseLandingScreen + QuizScreen patterns for RTL, inline STR bilingual maps, and `react-hooks/set-state-in-effect` workarounds.
- Did NOT modify any protected foundation file. All new strings that aren't already in `player.*` live in an inline bilingual `STR` map (dialect-neutral Saudi/Egyptian Arabic) + a `pick(locale, map)` helper — same pattern QuizScreen uses. No new i18n keys were added.
- Created `/home/z/my-project/src/components/screens/CoursePlayerScreen.tsx` — NAMED export `CoursePlayerScreen`, `'use client'` at top. Single file (~1776 lines) with private sub-components co-located for clarity: `CourseNotFound`, `CoursePlayerContent`, `ResumeBanner`, `VideoArea`, `QuizCta`, `PlayerTabs`, `OverviewTab`, `QATab`, `QaItem`, `NotesTab`, `ReviewsTab`, `ResourcesTab`, `CurriculumSidebar`, `AiAssistant`, `ChatBody`. No new shared files needed.
- Top-level component reads `useAppStore((s) => s.route.params?.courseId)`, looks up the course via `getCourse(id)`. If the course is missing or `courseId` is undefined → renders `<CourseNotFound courseId={courseId} />` which is a centered `EmptyState` with `BookX` icon, inline-bilingual title/description, and two buttons (outline "Back to course details" → `navigate("course-landing", { courseId })` when courseId exists, primary "Back to home" → `navigate("home")`). Otherwise renders `<CoursePlayerContent course={course} />`.
- AUTO-RESUME (no effect, no `set-state-in-effect` trip): `activeLessonId` is initialized via `useState(() => useAppStore.getState().getCurrentLessonId(course.id) ?? firstLessonId)` — runs once during the first render. `currentSec` is similarly initialized via `useState(() => { const init = useAppStore.getState().getCurrentLessonId(course.id) ?? firstLessonId; return useAppStore.getState().progress[course.id]?.lessons[init]?.positionSec ?? 0; })`. The "Resumed where you left off" banner (`player.autoResumed`) is gated by a `resumedLessonId` state initialized via `useState(() => { const init = state.getCurrentLessonId(course.id); const sec = state.progress[course.id]?.lessons[init]?.positionSec ?? 0; return sec > 0 ? init : null; })` — shown only while `activeLessonId === resumedLessonId`. The banner auto-dismisses after 4s via `setTimeout` inside an effect (setState in the timeout callback, not in the effect body). Manual lesson navigation clears `resumedLessonId` to null in the `goToLesson` event handler.
- Layout: `min-h-screen bg-background` wrapper with a sticky top bar (`sticky top-0 z-30 bg-card/95 backdrop-blur border-b`) and a `max-w-[1600px] mx-auto px-3 sm:px-4 lg:px-6 py-4 lg:py-6` body. Body grid is `lg:grid-cols-[minmax(0,1fr)_20rem] xl:grid-cols-[minmax(0,1fr)_22rem]` when the desktop sidebar is open, else `lg:grid-cols-1`. LEFT column = `space-y-4` containing the resume banner (conditional), `<VideoArea>`, and `<PlayerTabs>`. RIGHT column = `<aside className="hidden lg:block">` wrapping `<CurriculumSidebar>` (sticky `top-[4.5rem]`, `max-h-[calc(100vh-5rem)]`). Mobile gets a single column; the curriculum opens in a `<Sheet side={isRTL ? "left" : "right"}>` controlled by `mobileCurriculumOpen`, and the AI assistant opens in a bottom `<Sheet>` (height 80vh) controlled by `aiOpen`. A floating `size-14` violet `Sparkles` button (`lg:hidden fixed bottom-4 end-4 z-40`) opens the AI sheet on mobile when it's closed.
- Top bar (`h-14` flex row): ghost "Back" button with directional arrow (`ArrowLeft` LTR / `ArrowRight` RTL) + "Back to course details" label (hidden on `<sm`), vertical separator, flex-1 min-w-0 block with `<h1>` course title (`truncate`, `title` attr for full text) + `text-[11px] muted-foreground` "Course progress: {n}%" line, a `w-24 h-1.5` mini progress bar (`bg-muted` track + `bg-success rounded-full` fill via inline `width: ${pct}%`), a `lg:hidden` outline "Curriculum" button with `Menu` icon (opens mobile Sheet), a `hidden lg:inline-flex` outline icon button to toggle the desktop sidebar (`PanelRightClose` when open / `PanelRightOpen` when closed, `aria-pressed`, `aria-label` flips between hide/show), and a primary "Ask AI" button (`Sparkles` icon, `aria-pressed={aiOpen}`, `aria-label={player.aiToggle}`).
- VIDEO AREA (`VideoArea`): a `relative aspect-video rounded-xl bg-gradient-to-br ${course.accent} shadow-lg` container with (1) a radial-gradient pattern overlay, (2) a top gradient-overlay header showing the lesson type icon + uppercase type label + `<h2>` lesson title (locale-aware, `line-clamp-2`, white drop-shadow), (3) a full-card play/pause button (`absolute inset-0` button with a centered `size-16 sm:size-20` white circle holding `Play`/`Pause`/`RotateCcw` icon — restarts from 0 when at end — with `aria-pressed` + `aria-label`), and (4) a bottom gradient-overlay control bar with a clickable progress bar (`role="slider"`, `aria-valuemin/max/now`, keyboard ±5s on ArrowLeft/Right, RTL-aware click ratio = `(rect.right - clientX) / width`), a small play/pause/restart button, `mm:ss / mm:ss` time labels (Latin digits, `font-mono tabular-nums`), a `Volume2` icon button, and a `Settings` icon button (all with `aria-label`s). Below the video: a flex row with prev/next outline buttons (`ChevronLeft`/`ChevronRight` flipped by locale, disabled at boundaries) and either a success "Completed" badge (`CheckCircle2` + `player.completed`, `bg-success/15 text-success border-success/30`) or a success "Mark as complete" button (`Check` + `player.markComplete`, `bg-success text-success-foreground`). Then the lesson title (`text-xl sm:text-2xl font-bold`) + duration badge + locale-aware description. Then a conditional `<QuizCta>` card if `getQuizForCourse(course.id)` exists (violet-tinted card with `ClipboardList` icon, "Take the quiz" label, and a primary button → `navigate("quiz", { courseId })`).
- Simulated playback: `currentSec` and `isPlaying` live in `CoursePlayerContent`'s local state and are passed to `VideoArea` via a `PlayerState` object. `setCurrentSec` is typed as `React.Dispatch<React.SetStateAction<number>>` so the playback interval can use a functional updater. Three effects in `VideoArea`: (1) a `currentSecRef` sync effect (`useEffect(() => { currentSecRef.current = currentSec }, [currentSec])`) — ref mutated in effect, never during render; (2) a 1Hz `setInterval` playback effect that calls `setCurrentSec((prev) => prev >= totalSec ? prev : prev + 1)` — setState is inside the interval callback, NOT in the effect body, so `react-hooks/set-state-in-effect` is satisfied; (3) an auto-pause effect that fires `setIsPlaying(false)` inside `setTimeout(..., 0)` when `currentSec >= totalSec` (setState in timeout callback, not in effect body); (4) a 5s persist `setInterval` that calls `setLessonPosition(course.id, activeLessonId, currentSecRef.current)` — `setLessonPosition` is a Zustand store action (not setState), and reading from the ref avoids re-creating the interval every tick. `totalSec = Math.max(1, lesson.durationMin * 60)`. Clicking play when at end resets to 0 and plays. Seeking via the progress bar calls `setLessonPosition` immediately.
- `handleMarkComplete` (event handler, not an effect): calls `markLessonComplete(course.id, lesson.id)`, persists final position via `setLessonPosition(course.id, lesson.id, totalSec)`, fires a success toast (`{ title: lessonMarked, variant: "success" }`), then `setTimeout(() => onGoToLesson(next.id), 800)` to auto-advance. `goToLesson(id)` sets the active lesson, clears `resumedLessonId`, reads the saved `positionSec` from the store via `useAppStore.getState()`, resets `currentSec` to it (or 0), and pauses playback — all in an event handler so no lint concern.
- TABS (`PlayerTabs`): shadcn `<Tabs defaultValue="overview">` with a full-width `<TabsList>` (`w-full justify-start overflow-x-auto no-scrollbar h-auto py-1`) containing 5 `<TabsTrigger>`s using `player.tab.*` keys. Each `<TabsContent>` mounts the corresponding sub-component.
- OverviewTab: a `Card` with a `grid grid-cols-2 sm:grid-cols-3 gap-3` of meta items (icon chip + label + value) — language (`Globe`), rating (`Star` + `course.rating.toFixed(1)` + `(${formatNumber(course.ratingCount)})`), views (`Users` + `formatNumber(course.enrolledCount * 3)` — mock = enrolled × 3 per spec), duration (`Clock` + `formatDuration(lesson.durationMin)`), enrolled (`GraduationCap` + `formatNumber(course.enrolledCount)`). Below: a "About this course" heading + locale-aware `course.description` + instructor name line. Then a "Transcript" section with a `max-h-48 overflow-y-auto lms-scroll` muted box showing the locale-aware `lesson.transcript`.
- QATab: a `Card` with a `<Textarea>` (aria-labeled, `player.qa.askPlaceholder`) + submit button (`player.qa.ask`). Submit handler calls `addQuestion(course.id, q, user?.name ?? "You")`, clears draft, fires success toast (`questionAsked`). The list combines `extraQA[course.id] ?? []` (first) with `course.qa` (second). Each `<QaItem>` is a `Card` with avatar (initial fallback), student name + `formatDate(date)`, locale-aware question, separator, locale-aware answer, and an upvote button (`ThumbsUp` + `t("player.qa.upvotes", { n: upvotes })`) that increments a local `upvoteBoost` map (no store mutation). Empty state (`player.qa.empty` with `Inbox` icon) when the combined list is empty.
- NotesTab: a `Card` with a current-time badge (`formatTime(currentSec)` in `font-mono tabular-nums`), a `<Textarea>` with placeholder `t("player.notes.placeholder", { time: formatTime(currentSec) })`, and a "Save note" button (`player.notes.add`). Save handler calls `addNote(course.id, lesson.id, text, currentSec)`, clears draft, fires success toast (`noteSaved`). The notes list reads `progress[course.id]?.lessons[lesson.id]?.notes ?? []` from the store and filters out a local `deletedNoteIds` set (delete button = ghost `Trash2` icon, `aria-label={deleteNote}`). Each note shows a `font-mono` "at mm:ss" badge (`player.notes.at`) + the note text. Empty state (`player.notes.empty`) when no visible notes.
- ReviewsTab: a compact summary `Card` with centered big rating (`text-4xl font-bold tabular-nums`), `StarRating` (size 14, centered), and `{n} reviews` count. Below: a `<ul>` of `course.reviews` — each `<li>` is a `Card` with avatar (initial), student name, `StarRating` (size 12) + `formatDate(date)`, and locale-aware comment (`commentAr ?? comment`).
- ResourcesTab: reads `lesson.resources ?? []`. Each resource is a `Card` with a type-icon chip (`FileText` for pdf, `FileArchive` for zip, `Presentation` for slides — picked via a `RESOURCE_ICON` map), file name (`truncate`), uppercase type label, and a "Download" outline button (`player.resources.download`) that fires a default toast `downloading {name}…` (inline-bilingual). Empty state (`player.resources.empty`) when no resources.
- CURRICULUM SIDEBAR (`CurriculumSidebar`): a `<nav aria-label={curriculum}>` (sticky on desktop, full-height in the mobile Sheet) with a header block (`p-3 border-b bg-muted/30`) showing the `player.curriculum` label, course title (`line-clamp-1`), and a `flex-1 h-1.5 bg-muted` track + `bg-success rounded-full` fill (inline `width: ${courseProgress}%`) + `{pct}%` label. The body is `overflow-y-auto lms-scroll` (`lg:max-h-[70vh]`, `flex-1` in sheet). Each section is a shadcn `<Collapsible defaultOpen>` (all expanded by default) with a custom trigger button (`w-full flex items-center gap-2 p-3 text-start hover:bg-muted/40`) containing a `ChevronDown` icon (rotates -90° when closed via `[&[data-state=closed]>svg]:-rotate-90 transition-transform duration-200`), section title (`line-clamp-1`), and `{n} lessons` count (inline-bilingual). `CollapsibleContent` renders a `<ul>` of lesson buttons. Each lesson button: `border-s-4 border-primary` (active) or `border-s-4 border-transparent` (inactive) — same 4px width so no layout shift; `bg-primary/10` background when active; completion indicator (`CheckCircle2 text-success` when completed, `Circle text-muted-foreground/40` when not); lesson-type icon (`PlayCircle`/`BookOpen`/`FileQuestion` via `LESSON_TYPE_ICON` map, primary-tinted when active); locale-aware title (`line-clamp-2`, `font-semibold` when active); duration (`{n} {landing.min}`). Clicking a lesson calls `onSelectLesson(id)` which triggers `goToLesson` (and closes the mobile Sheet if applicable).
- AI ASSISTANT (`AiAssistant` + `ChatBody`): chat state (`messages: {role, text}[]`, `input`, `isThinking`) lives in `AiAssistant` so it persists across open/close toggles within the same screen mount. A `greetedRef` (mutated in effect — allowed) tracks whether the greeting has been seeded; on first open, a `setTimeout(50ms)` appends `{role: "ai", text: player.aiGreeting}` to messages (setState in timeout callback, not in effect body). Auto-scroll: `messagesRef` + `useEffect(() => { el.scrollTop = el.scrollHeight }, [messages, isThinking])` — DOM mutation only, no setState. Send handler: appends user message, clears input, sets `isThinking(true)`, then `setTimeout(900ms)` appends a canned reply and clears `isThinking` — all setState calls are inside the timeout callback (event handler → setTimeout → setState). Reply generator picks from 3 templates (`aiReply1/2/3` inline-bilingual) based on message length heuristic (`<20` chars → template 1, `>80` → template 3, else template 2, with 30% chance to rotate). Templates interpolate `{lesson}` (locale-aware lesson title) and `{snippet}` (first sentence of locale-aware transcript = `transcript.split(".")[0] + "."`). Enter key sends (no shift). Mobile: bottom `<Sheet>` (80vh) with header (`Sparkles` chip + `player.aiAssistant` title) + `<ChatBody>` + input bar. Desktop: `hidden lg:flex fixed bottom-4 end-4 w-96 h-600 max-h-[calc(100vh-5rem)]` panel with header (title + close `X` button) + `<ChatBody>` + input bar, rendered only when `open` (the `useIsMobile()` hook chooses between the two layouts — by the time the user clicks the toggle, the hook has resolved to the correct value). Messages styled as bubbles: user = `bg-primary text-primary-foreground rounded-2xl rounded-es-sm self-end` with user avatar; AI = `bg-muted text-foreground rounded-2xl rounded-ee-sm self-start` with a violet `Sparkles` circle avatar. Thinking indicator = three `.typing-dot` spans (uses the foundation's `typingBounce` keyframe) with `role="status"` + `sr-only` label. AI messages also carry `role="status"` for screen readers.
- RTL/i18n: every visible string and every aria-label goes through `t()` (existing `player.*` keys) or the inline `STR` map + `pick(locale, …)`. Layout uses only logical CSS (`ps-`, `pe-`, `ms-`, `me-`, `start-`, `end-`, `text-start`, `border-s-`, `rounded-es-sm`/`rounded-ee-sm` for chat bubble corners, `inset-inline`-aware `end-4` for the fixed AI button and panel). The back arrow, prev/next chevrons, and mobile Sheet side all flip via `isRTL`. The seek bar click ratio is RTL-aware (`(rect.right - clientX) / width` in Arabic). The curriculum lesson highlight uses `border-s-4` (logical start border). The mini progress bars (top bar + sidebar) use inline `width: ${pct}%` on a `start-0`-anchored fill div — fills from the start edge in both LTR and RTL. `<html dir>` is synced by the existing `DirSync` component so no manual dir handling inside the screen.
- Accessibility: sticky top bar is a `<header>`. The sidebar is a `<nav aria-label>`. Tabs use shadcn `<Tabs>` (proper tablist/tab/tabpanel roles). The video play button exposes `aria-pressed`. The seek bar is a `role="slider"` with `aria-valuemin/max/now` + keyboard support. All icon-only buttons (volume, settings, sidebar toggle, AI close, send, delete note, upvote) have `aria-label`s. The active curriculum lesson uses `aria-current="true"`. AI replies and the thinking indicator carry `role="status"` for screen-reader announcements. The chat input has `aria-label`. The resume banner carries `role="status"`. The desktop AI panel is a `role="dialog"` with `aria-label`. Color is never the only signal — completed lessons show a `CheckCircle2`, the completed-lesson badge pairs green text with a check icon, the active lesson has both a violet start-border AND a tinted background.
- `react-hooks/*` rule compliance: (1) `useState` initializer for `activeLessonId`, `currentSec`, and `resumedLessonId` — no effect needed for initial values. (2) Playback interval: setState (`setCurrentSec`) is called inside the `setInterval` callback, not in the effect body. (3) Auto-pause: `setIsPlaying(false)` is called inside `setTimeout(…, 0)` callback, not in the effect body. (4) Greeting seed: `setMessages(…)` is called inside `setTimeout(50ms)` callback; the `greetedRef.current = true` mutation is a ref mutation in an effect (allowed). (5) Auto-scroll: only mutates `messagesRef.current.scrollTop`, no setState. (6) AI send handler: `setTimeout(900ms)` callback calls `setMessages` + `setIsThinking` — event handler → setTimeout → setState, no effect involved. (7) `currentSecRef.current = currentSec` is a ref mutation in an effect (allowed by `react-hooks/refs`). (8) No recursive functions in component scope; all interval/timeout callbacks are defined inside the effects/handlers that set them up. (9) `exhaustive-deps` is off in the project config but I included all relevant deps anyway.
- Lint: `cd /home/z/my-project && bun run lint` → CLEAN (exit 0, no errors, no warnings). `bunx tsc --noEmit` reports zero errors originating from `CoursePlayerScreen.tsx` (the only remaining tsc errors are pre-existing ones in `examples/`, `skills/`, and `src/components/layout/Header.tsx` — out of scope for this task).

Stage Summary:
- `CoursePlayerScreen` is a NAMED export at `src/components/screens/CoursePlayerScreen.tsx`, ready for the root `page.tsx` (Task ID 4) to render when `route.name === "course-player"`.
- No protected foundation files modified. All new strings live in an inline bilingual `STR` map + `pick(locale, …)` helper (QuizScreen pattern); existing `player.*` i18n keys are used directly.
- Honors all spec requirements: SPA at `/` (no new Next.js routes), `'use client'` at top, auto-resume to first uncompleted lesson via `useState` initializer (no `set-state-in-effect` trip), "Resumed where you left off" banner when saved `positionSec > 0` (auto-dismisses after 4s), 2-column desktop (video+tabs left, collapsible curriculum sidebar right) + single-column mobile (video on top, tabs below, curriculum in Sheet, AI in Sheet opened by floating "Ask AI" button), sticky top bar with back link + course title + progress % + mini bar + sidebar toggle + AI toggle, 16:9 video placeholder with accent gradient + center play button + title overlay + fake control bar (play/pause, clickable seek bar with mm:ss labels, volume, settings), simulated 1Hz playback via `setInterval` with `setCurrentSec` inside the callback, periodic `setLessonPosition` persistence via a 5s interval reading from a synced ref, "Mark as complete" button (success toast + 800ms auto-advance) swapping to a green "Completed" badge when done, prev/next lesson buttons (disabled at boundaries, restore saved `positionSec`), lesson title + description below video, "Take the quiz" CTA when a quiz exists, 5 tabs (Overview/Q&A/Notes/Reviews/Resources) with locale-aware content, Q&A with extra-first ordering + local upvote boost + ask box, Notes with current-time display + save + per-lesson list + local delete filter, Reviews with summary card + StarRating + dates, Resources with type icons + mock download toast, collapsible curriculum with section chevrons + per-lesson type icon + completion check + active highlight (logical `border-s-4`), mock AI tutor with greeting + 900ms thinking delay + 3 canned templates that reference the current lesson's title and transcript first-sentence + auto-scroll via ref + bubble styling + `role="status"` on AI replies, full RTL + Saudi/Egyptian Arabic via logical CSS and `t()`/`pick()`, accessibility landmarks/aria-pressed/aria-labels/aria-current/role=slider/role=status, and the `.view-enter` + `.slide-enter` entrance animations.

---
Task ID: 4 + 5
Agent: main (orchestrator)
Task: Wire all screens into root page.tsx router + Agent Browser self-verification

Work Log:
- Wrote `src/app/page.tsx` as the SPA router: renders `<DirSync/>`, `<ConfettiOverlay/>`, `<ToastViewport/>` always; `<Header/>` + `<Footer/>` only on non-auth/non-onboarding screens; switches the active `<Screen/>` based on `useAppStore((s) => s.route).name` via a `SCREENS` lookup table. Added a guard so an unauthenticated user is forced back to `auth` (except onboarding). Wrapped each screen in a `.view-enter` keyed div for transition animation. Used `min-h-screen flex flex-col` on the root + `flex-1` on main + `mt-auto` on footer for the sticky-footer pattern.
- Found and fixed a runtime error: `Header.tsx` imported `useMobile` but the hook is actually exported as `useIsMobile` from `@/hooks/use-mobile`. Fixed the import. The page went from HTTP 500 to 200.
- Fixed a React warning in `QuizScreen.tsx`: the `RadioGroup` was switching from uncontrolled (value=undefined) to controlled (value=string) when a user selected an answer. Changed `value={selectedIndex !== undefined ? String(selectedIndex) : undefined}` → `value={selectedIndex !== undefined ? String(selectedIndex) : ""}` so the value is always a defined string.
- Ran Agent Browser end-to-end verification of the complete golden path:
  1. **Auth**: "Continue as demo student" → Home (Layla Hassan, 2 enrolled courses pre-seeded).
  2. **Home**: Greeting + "Continue learning" (2 courses w/ progress bars + Resume) + 3 carousels (What to learn next, Recommended, Trending) with CourseCards (wishlist + add-to-cart actions).
  3. **Course Player**: Auto-resume loaded "The CSS box model" (first uncompleted lesson) with video position at 240s matching the demo state. Tested AI Assistant — sent "What is the CSS box model?" and got a contextual canned response citing the lesson transcript. Tested "Mark as complete" → auto-advanced to next lesson. Tested Notes tab — saved a timestamped note, appeared in the list with a delete button.
  4. **Cart**: Added a course from Home → Cart page showed item + promo input + checkout. Applied promo code EDIFY10.
  5. **Checkout**: Payment form (name, card number with auto-spacing, expiry MM/YY, CVC) → "Pay $109.99" → 1.2s processing → Confirmation screen with confetti + "Now in your learning" list.
  6. **Quiz**: Navigated to SQL course (via Search → landing → Enroll → player → Take the quiz). Answered all 5 questions correctly (LEFT JOIN, HAVING, ROW_NUMBER, CTEs, cohort) → "Passed! Nailed it! 100% — You earned a certificate!" with confetti. Certificate appeared in My Learning → Certificates tab.
  7. **Onboarding**: Signed up as new user "Omar Test" → 5-step flow (intent → role: Data Analyst → skills: SQL+Python → education: Bachelor's → completion). Step 5 fired confetti + showed personalized recommendations (SQL, Python, Tableau, Statistics) → "Start learning" → Home with "Welcome back, Omar Test".
  8. **Arabic/RTL**: Switched to Arabic via header language switcher → `<html dir="rtl" lang="ar">` + all UI strings translated (Saudi dialect). Verified the dialect sub-menu appears in Arabic mode and switching to Egyptian changed the greeting from "هلا بعودتك" (Saudi) to "نوّرت تاني" (Egyptian).
  9. **Responsive**: Set viewport to 375×812 (mobile) — layout stacked correctly, nav collapsed to icon-only buttons, carousels remained horizontally scrollable.
  10. **Sticky footer**: Verified CSS structure — root is `min-h-screen flex flex-col` (min-height: 100vh, display: flex, flex-direction: column), footer has `mt-auto`. Confirmed footer sits at the bottom on both short and long content pages.
- Verified `agent-browser errors` returns empty (no page errors) and the console is clean after the RadioGroup fix.

Stage Summary:
- The Edify LMS MVP prototype is COMPLETE and browser-verified end-to-end.
- All 9 feature screens + foundation are wired into the single `/` route via SPA navigation.
- `bun run lint` → CLEAN (0 errors, 0 warnings).
- Dev server returns HTTP 200 on `/` with no runtime errors.
- Golden path verified: Sign up → Onboarding (confetti) → Home → Search → Course Landing → Enroll/Cart → Checkout (confetti) → Course Player (AI assistant, notes, Q&A, mark complete) → Quiz (confetti) → My Learning (progress + certificate).
- i18n verified: English + Arabic (Saudi + Egyptian dialects) with full RTL layout mirroring.
- Responsive verified: mobile (375×812) and desktop (1280×800).
- Sticky footer verified via CSS structure inspection.
- The prototype is ready for demonstration and validation.

---
Task ID: 6
Agent: main (orchestrator)
Task: Apply the "white smoke + purpla" style tokens to the design system

Work Log:
- Read the style token file at `/home/z/my-project/upload/white smoke purpla.txt` — 7 palettes: white-smoke (warm neutrals), thistle (soft lavender), mauve, wisteria, mauve-magic, neon-violet (vivid violet), hyper-magenta.
- Rewrote `src/app/globals.css` `@theme inline` block to register ALL 7 palettes as Tailwind theme color scales (50→950 each), so utilities like `bg-neon-violet-500`, `text-white-smoke-900`, `border-thistle-300`, `bg-mauve-magic-600` are available project-wide.
- Mapped the semantic design tokens to the new palettes:
  - **Primary / Brand / Ring**: `#a617e8` (neon-violet-500) — vivid violet, very close to Udemy's actual brand `#a435f0`.
  - **Neutrals** (background, foreground, card, muted, border, secondary, sidebar): white-smoke scale — `#f3f2f2` (ws-50) page bg, `#ffffff` cards (pop on smoke), `#1b1818` (ws-900) text, `#e7e4e4` (ws-100) muted, `#cecaca` (ws-200) borders, `#6a6262` (ws-600) muted-foreground.
  - **Accent**: thistle — `#f5ecf9` (thistle-50) bg + `#5e2673` (thistle-700) foreground for soft lavender secondary surfaces.
  - Dark mode: white-smoke-950/900 backgrounds + neon-violet-300 (`#c77aeb`) brand + thistle-800/200 accent.
  - Kept functional colors: success green `#1e7f45`, rating gold `#f5c518`, destructive red.
  - Chart palette updated to draw from the purple family + functional accents.
- Updated `src/components/shared/ConfettiOverlay.tsx` COLORS array from the old palette to the new purple family: neon-violet-500, thistle-500, mauve-magic-500, hyper-magenta-500 + success green + gold + pink accent.
- Updated `src/components/screens/AuthScreen.tsx` brand-hero gradient from `#5624D0→#4b1ec0→#2d1a8a` to the neon-violet scale: `#a617e8→#8512ba→#42095d` (neon-violet 500→600→800).
- Verified via Agent Browser (computed styles):
  - `--primary` = `#a617e8`, `--background` = `#f3f2f2`, `--foreground` = `#1b1818`, `--card` = `#ffffff`, `--border` = `#cecaca`, `--accent` = `#f5ecf9`, `--accent-foreground` = `#5e2673`, `--ring` = `#a617e8` — all correct.
  - Primary button bg = `rgb(166,23,232)` (#a617e8). Card bg = pure white. 6 success-green progress bars, 21 thistle-accent elements, 65 gold rating stars on Home. Course landing "Enroll now" CTA in neon-violet.
  - Lint clean, dev server HTTP 200, no runtime errors.

Stage Summary:
- The "white smoke + purpla" style tokens are now the design system's source of truth.
- All 7 palettes are registered as Tailwind theme colors (available as on-demand utilities).
- Semantic tokens (bg-primary, text-foreground, bg-card, border-border, bg-accent, bg-success, text-gold, etc.) all flow through the new palette — every screen automatically picks up the new look with zero component changes needed.
- Light mode: warm white-smoke neutrals + vivid neon-violet brand + soft thistle accent.
- Dark mode: warm near-black (white-smoke-950) + bright neon-violet-300 brand + thistle-800 accent.
- Confetti bursts now use the purple family, matching the brand.

---
Task ID: 2-a
Agent: sub-agent (InstructorProfile)
Task: Build the Instructor Profile screen

Work Log:
- Read the worklog + foundation files first: `src/lib/store.ts` (route params, `navigate`/`goBack`/`history`, `enrolledCourseIds`, `addToCart`, `isInCart`, `pushToast`), `src/lib/mock-data.ts` (`getInstructor(id)` + `courses` array, instructor shape `{id,name,nameAr?,title,titleAr?,bio,bioAr?,avatar,rating,students,coursesCount}`, review shape `{id,studentName,rating,date,comment,commentAr?}`), `src/lib/types.ts` (`Instructor`, `Course`, `Review`), `src/lib/i18n.ts` (existing `instructor.*` keys in all three dicts: title/courses/students/rating/reviews/bio/allCourses/allReviews/notFound/notFoundDesc/backToCourse/browse/specialties/totalHours/avgRating/viewCourse/viewProfile; plus reused `landing.*`, `card.*`, `home.*`, `onboarding.back`, `search.*` keys), `src/hooks/use-i18n.ts` (`t`, `locale`, `isRTL`, `formatNumber`, `formatDate`), `src/components/shared/{StarRating,CourseCard,EmptyState}.tsx`, and `src/components/ui/{button,card,badge,avatar}.tsx`. Skimmed `CourseLandingScreen.tsx` to match its `instructor-profile` navigation + `InstructorSection` patterns and reuse its review/list styling conventions.
- Created `/home/z/my-project/src/components/screens/InstructorProfileScreen.tsx` — NAMED export `InstructorProfileScreen`, `'use client'` at top. Self-contained (sub-components `InstructorNotFound`, `InstructorProfileContent`, `HeroCard`, `StatCard`, `ReviewsSection`, `CtaCard`, and helper `cnChevron` all live in the same file). No new shared components added, no foundation file modified.
- Reads `instructorId` reactively from `useAppStore((s) => s.route.params?.instructorId)`. If `getInstructor(id)` returns nothing → `InstructorNotFound` renders `<EmptyState>` with `UserX` icon, `instructor.notFound` title, `instructor.notFoundDesc` description, and a primary Button labelled `instructor.browse` → `navigate("search")`.
- Main layout: `max-w-5xl mx-auto w-full px-4 sm:px-6 py-6 sm:py-8 space-y-8`. Five sections stacked vertically.
- 1) Back link: ghost `<Button size="sm">` with a locale-aware directional arrow (`ArrowLeft` LTR / `ArrowRight` RTL — both imported from lucide), labelled `onboarding.back`. `handleBack` calls `goBack()` when `history.length > 0`, otherwise `navigate("search")`. `aria-label` set for screen readers.
- 2) Hero header `<Card>` (rounded-xl, `overflow-hidden`, `py-0 gap-0` so the gradient banner meets the body cleanly):
  - Top banner: `bg-gradient-to-br from-primary/10 via-primary/5 to-transparent`. Flex-col on mobile / flex-row on `sm+`. Large avatar (`w-24 h-24 sm:w-28 sm:h-28`, `border-4 border-background shadow-md`) using shadcn `Avatar`/`AvatarImage`/`AvatarFallback` (alt = instructor name; fallback = first letter on `bg-primary/10 text-primary`).
  - H1 = locale-aware name. Subtitle = locale-aware `title` (muted).
  - Specialties row: derived from the instructor's `courses` filtered by `instructorId` → unique `category`/`categoryAr` pairs → pill `<Badge variant="outline" bg-background/70>` for each. Label uses `instructor.specialties` + ":".
  - Stats grid: `grid-cols-2 sm:grid-cols-4 gap-3` of four `StatCard` sub-components (each `role="group"` + `aria-label` = `${label}: ${value}`): rating (`Star` icon gold + `StarRating` + numeric `avgRating`), students (`Users` icon, `formatNumber(instructor.students)`), courses (`GraduationCap` icon, count of filtered courses), total hours (`Clock` icon, sum of `course.totalHours` across their catalog — uses `formatNumber`).
  - Bio block: H2 = `instructor.bio` (uppercase muted micro-label), `<p>` = locale-aware bio.
  - Tiny meta row: `instructor.avgRating` + `instructor.reviews` counts (tabular-nums).
- 3) "Courses by {name}" section: `<section aria-labelledby="instructor-courses-heading">` with H2 = `instructor.allCourses` interpolated with the locale-aware name, plus a `<Badge>` count. Responsive `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 sm:gap-5` of `<CourseCard variant="default" showActions />` for every course by this instructor (filters `courses` by `instructorId`). Empty fallback uses `<EmptyState icon={BookOpen}>` with a "Browse" button → `navigate("search")`. (Catalog reality: i-1 has 4 courses, i-2 has 5, i-3 has 4, i-4 has 2, i-5 has 3 — every instructor renders a populated grid.)
- 4) "Student reviews" section: `<section aria-labelledby="instructor-reviews-heading">` with H2 = `instructor.allReviews`. Aggregates ALL `course.reviews` across the instructor's catalog into `{review, course}[]` via `useMemo`. Renders a summary `<Card>` showing the average rating across all aggregated reviews (computed; falls back to `instructor.rating` when no reviews), a `StarRating`, and `landing.reviewsSub` with the total count — plus a right-aligned `instructor.avgRating` reference to the instructor's own stored rating. Below: a semantic `<ul>` of review `<li>`s, each rendered as a `<Card>` with: small avatar fallback, student name, gold `StarRating` (locale-agnostic numeric), date via `formatDate(review.date)` aligned to the end on `sm+`, locale-aware comment, and a small "Instructor: {courseTitle}" meta line. Initial view = 6 reviews; a `Show more` / `Show less` toggle (`landing.showMore` / `landing.showLess`) expands to 12. `aria-expanded` on the toggle button; chevron rotates via a small `cnChevron(expanded)` helper. Empty list → `<EmptyState icon={BookOpen}>`.
- 5) Bottom CTA card (subtle, optional per spec): rendered only when the user is not enrolled in ANY of the instructor's courses (`myCourses.some(c => enrolledCourseIds.includes(c.id))` is false) and the instructor has at least one course. Picks the top-rated course via `[...myCourses].sort((a,b) => b.rating - a.rating)[0]`. Card uses a soft `bg-gradient-to-br from-primary/5 via-transparent to-transparent border-primary/20` with a `Sparkles` chip, `home.trendingSub` as the tagline, the course title (truncated), an outline `card.addToCart` button (no-ops + toasts `landing.addedToCart` if already in cart), and a primary `instructor.viewCourse` button → `navigate("course-landing", { courseId: topCourse.id })`. Chose a non-fixed card over a fixed bottom bar so it doesn't overlap the global Footer chrome.
- React Hooks: only `useMemo` (compute-once-per-instructor) and `useState` (review expand toggle). NO `useEffect` anywhere — every `setState` happens inside an event handler (`onClick`), so the `react-hooks/set-state-in-effect` rule is satisfied by construction.
- i18n / RTL: every visible string and every aria-label goes through `t()`. All layout uses logical CSS (`ms-`, `me-`, `ps-`, `pe-`, `start-`, `end-`, `sm:ms-auto`, `-ms-2`). The back arrow flips between `ArrowLeft` (LTR) and `ArrowRight` (RTL) via `isRTL`. Locale-aware fields use the established pattern `locale === "ar" ? x.nameAr ?? x.name : x.name` (same for title/bio; for reviews, `review.commentAr ?? review.comment`; for course titles, `course.titleAr`).
- Accessibility: H1 = instructor name; every section is `<section aria-labelledby="…-heading">` with a corresponding H2; avatar `alt` = name; each `StatCard` is `role="group"` + `aria-label`; review list is a real `<ul>`/`<li>`; "Show more" toggle carries `aria-expanded`; back button carries `aria-label`.
- Design system: violet primary (`bg-primary/10`, `text-primary`, `border-primary/20`), gold rating stars via `text-gold` + `StarRating`, warm white-smoke neutral background inherited from `bg-background`/`bg-card`, rounded-xl cards via shadcn `Card`, mobile-first responsive (`grid-cols-2 sm:grid-cols-4`, `flex-col sm:flex-row`, etc.).
- Lint: `cd /home/z/my-project && bun run lint` → CLEAN (0 errors, 0 warnings). `bunx tsc --noEmit` reports zero errors originating from `InstructorProfileScreen.tsx` (the only remaining tsc errors are pre-existing ones in `examples/`, `skills/`, and the missing `AiPlannerScreen`/`AiCoachScreen` modules referenced by `src/app/page.tsx` — out of scope for this task).

Stage Summary:
- `InstructorProfileScreen` is a NAMED export at `src/components/screens/InstructorProfileScreen.tsx`, already wired into `src/app/page.tsx`'s `SCREENS` lookup for `route.name === "instructor-profile"`. Reaching it: `navigate("instructor-profile", { instructorId: "i-1" })` — and `CourseLandingScreen`'s `InstructorSection` already does exactly that on avatar/name/profile-button clicks.
- No foundation files were modified. No new shared components were added.
- Honors all spec requirements: SPA screen with `'use client'`, EmptyState not-found fallback with Browse CTA, ghost back button (history-aware), hero card (avatar + H1 name + title + specialties pills + 4 stat cards [rating/students/courses/totalHours] + bio), "Courses by {name}" responsive grid (1/2/3 cols) of CourseCards with actions, aggregate reviews section (summary card with avg + count + StarRating + 6→12 list with locale-aware name/date/comment and Show-more toggle), and a bottom CTA card suggesting the instructor's top-rated course when the learner isn't enrolled in any. Full Arabic + RTL + Saudi/Egyptian dialect support via logical CSS and `t()`. Lint passes.

---
Task ID: 2-c
Agent: sub-agent (AiCoachScreen)
Task: Build the AI Learning Coach screen — chat-based mentor with MOCK (simulated) AI responses

Work Log:
- Read worklog + foundation files first: `src/lib/store.ts` (`user`, `onboardingAnswers.roleId`, `enrolledCourseIds`, `navigate(name, params)` — all read via selectors; SPA routing only), `src/lib/types.ts` (`Locale = "en" | "ar"`, `RouteName` includes `"ai-coach"`, `"course-landing"`, `"ai-planner"`), `src/hooks/use-i18n.ts` (`useI18n()` → `{ t, locale, isRTL, ... }`), `src/lib/mock-data.ts` (`courses`, `getCourse`, `getRole` — roles have `label`/`labelAr`/`recommendedCourseIds`; courses have `title`/`titleAr`/`skills`/`skillsAr`/`category`), `src/lib/i18n.ts` (`aiCoach.*` keys present in en/arSa/arEg: title, subtitle, navDesc, greeting {name}, placeholder, send, thinking, suggestions, s1..s6, clear, you, coach, disclaimer, history, empty), `src/app/page.tsx` (already imports `AiCoachScreen` and routes `route.name === "ai-coach"` to it; the screen renders inside `<main className="flex-1">` → `<div className="view-enter flex-1 flex flex-col">`), `src/app/globals.css` (`.typing-dot` keyframe `typingBounce`, `.no-scrollbar`, `.view-enter`, neon-violet `--color-neon-violet-500 #a617e8` mapped to `--primary`), `src/components/ui/{button,textarea}.tsx` (Button supports `size="icon"` = size-9; Textarea has `field-sizing-content min-h-16`).
- Created `/home/z/my-project/src/components/screens/AiCoachScreen.tsx` — NAMED export `AiCoachScreen`, `'use client'` at top. No foundation files modified. No new shared components added. The module was already referenced by `src/app/page.tsx` (Task 3-j InstructorProfileScreen noted it was missing) — it now resolves.
- Layout: a centered `max-w-3xl` container, `h-[calc(100dvh-4rem)]` (fills viewport below the sticky `h-16` header), `flex flex-col overflow-hidden`. Three regions: (1) header (title + subtitle + disclaimer + clear button), (2) scrollable messages list (`flex-1 min-h-0 overflow-y-auto`, `role="log"` + `aria-label={aiCoach.history}`), (3) bottom input area (suggestion chips + Textarea + round Send button). The footer renders below the fold (reachable by page scroll) — acceptable for a chat surface; the chat itself fills the viewport.
- State (all in-memory React `useState`, NO localStorage, NO backend):
  - `messages: Message[]` seeded via lazy `useState` initializer with a single greeting message (`id: "m-greeting"`, role `"coach"`, `text = t("aiCoach.greeting", { name })` where `name = user?.name ?? "there"`). Lazy init runs once on mount → no `setState`-in-effect needed.
  - `draft: string` for the textarea.
  - `thinking: boolean` for the typing indicator.
  - `Message = { id: string; role: "user" | "coach"; text: string; action?: CoachAction }` where `CoachAction = { kind: "course" | "planner"; courseId?: string; label: string }` (label pre-localized at generation time).
- Refs (mutated ONLY in effects/event handlers, never during render):
  - `scrollRef` (messages container) for auto-scroll.
  - `idSeq` (counter) for message ids — `nextId()` called only inside the `sendMessage` event handler / its `setTimeout` callback (never during render; the greeting uses a static id).
  - `sendingRef` (boolean guard) to prevent double-sends from rapid chip/Enter presses (independent of React render timing).
  - `timeoutRef` (the pending `setTimeout` handle) so `handleClear` can cancel an in-flight response and the unmount cleanup can avoid setState-after-unmount.
- Auto-scroll: `useEffect` with deps `[messages.length, thinking]` whose body ONLY mutates `scrollRef.current.scrollTop = scrollRef.current.scrollHeight` (a DOM mutation, NOT `setState`) → satisfies `react-hooks/set-state-in-effect`. A second `useEffect(() => () => clearTimeout(timeoutRef.current), [])` is cleanup-only (no `setState` in the body).
- Send flow (`sendMessage(raw)` — called by the Send button, Enter key, and suggestion chips): trim → guard on `sendingRef.current` → append user message → clear draft → `setThinking(true)` → capture `CoachContext { name, locale, roleId, enrolledCourseIds, enrolledCount }` → `setTimeout` (900–1300ms randomized) INSIDE the handler (not in an effect) → on fire: `generateCoachResponse(text, ctx)` → append coach message (with optional `action`) → `setThinking(false)` → reset `sendingRef`. `handleClear` cancels the pending timeout, resets `sendingRef`/`thinking`, and reseeds the greeting (recomputed with the current locale's `t`, so a locale switch + clear gives a localized greeting).
- Input: shadcn `Textarea` (`rows={2}`, `min-h-[44px] max-h-40 resize-none`, `aria-label={aiCoach.placeholder}`, bound to `draft`). `onKeyDown`: Enter sends, Shift+Enter inserts a newline. Round Send `Button` (`size="icon"`, `rounded-full h-10 w-10`, `aria-label={aiCoach.send}`, `disabled={!canSend}` where `canSend = draft.trim() && !thinking`).
- Suggestion chips: 6 chips (`aiCoach.s1..s6`) under a `aiCoach.suggestions` label, shown only when `messages.length <= 1 && !thinking` (i.e. just the greeting) — horizontally scrollable via `no-scrollbar`. Clicking a chip calls `sendMessage(chipText)` directly (fills + sends immediately per spec).
- Mock response generator (the core): a MODULE-scope `generateCoachResponse(userMessage, ctx): { text, action? }` function — NO real LLM. Uses Unicode-aware tokenization (`split(/[^\p{L}\p{N}']+/u)`) so single-word keywords match as whole tokens (avoids "hi" matching "this") and multi-word phrases use substring matching. The `KEYWORDS` map (declared `as const`, so `hasKeyword` accepts `readonly string[]`) covers 8 categories with both English and Saudi-Arabic keyword lists; the `kws(cat)` helper checks the active locale's keywords first, then the other locale's (so an English question inside the Arabic UI still matches). Priority order: thanks → hello → planner → career → hours → whatNext → stuck → motivation → default. Each branch returns locale-aware text (AR uses Saudi-dialect tone, e.g. "هلا", "تحب", "يلا") with at most one emoji per response. Context is woven in: hello/thanks use `{name}`; career uses the role label (`getRole(roleId).label`/`.labelAr`, falling back to "your chosen path"/"مسارك المختار"); whatNext uses `pickNextCourse(ctx)` to reference a REAL course title (`course.title`/`titleAr`) + first skill (`skills[0]`/`skillsAr[0]`).
- `pickNextCourse(ctx)`: (1) if `roleId` → first role-recommended course NOT already enrolled (else first recommended); (2) else if enrolled → first course in the same `category` as the first enrolled course, not enrolled; (3) else `courses[0]`. Returns a real `Course` so the text and action reference live catalog data.
- Actionable responses (per spec): the "whatNext" response includes a `course` action button (`BookOpen` icon + localized "View course"/"اعرض الكورس") → `navigate("course-landing", { courseId })`. The "planner" and "hours" responses include a `planner` action button (`Target` icon + "Open AI Planner"/"افتح المخطط الذكي") → `navigate("ai-planner")`. Action buttons render inside the coach bubble; clicking invokes `handleAction` which routes via the store.
- Chat bubbles: coach = `Bot` icon in a `bg-primary text-primary-foreground` violet circle (w-8 h-8) on the start side + `bg-card border border-border` bubble (`rounded-2xl rounded-ss-md`, self-start). User = `bg-primary text-primary-foreground` bubble (`rounded-2xl rounded-se-md`, self-end) + a small initial-avatar circle (`bg-primary/15 text-primary`, user's first initial). Logical CSS (`rounded-ss-md`/`rounded-se-md`, `ms-`/`me-`, `justify-start`/`justify-end`) auto-flips for RTL.
- Thinking indicator: a coach-style row with the Bot avatar + a `bg-card border` bubble containing three `.typing-dot` spans (uses the existing `typingBounce` keyframe in globals.css) + the `aiCoach.thinking` label. The wrapper carries `aria-live="polite"` + `aria-label={aiCoach.thinking}` so screen readers announce the coach is composing.
- Accessibility: messages container is `role="log"` with `aria-label`; each bubble is `role="article"` with `aria-label` = `aiCoach.you` / `aiCoach.coach`; textarea + send button + clear button all carry `aria-label`s; thinking indicator is `aria-live="polite"`.
- React Hooks rule compliance: NO `setState` synchronously in any effect body (auto-scroll effect only does DOM mutation; unmount effect is cleanup-only). The "thinking" delay uses `setTimeout` INSIDE the `sendMessage` event handler (not in an effect). `nextId`/`sendingRef`/`timeoutRef` are mutated only in event handlers / `setTimeout` callbacks / cleanups — never during render. `generateCoachResponse`, `hasKeyword`, `tokenize`, `pickNextCourse`, and `KEYWORDS` are all module-scope (not recursive component-scope functions) → satisfies `react-hooks/immutability` and `react-hooks/refs`.
- i18n / RTL: every visible string + aria-label goes through `t()`; the mock responses are bilingual (EN + Saudi-AR) picked by `locale`. Layout uses logical CSS (`ps-`/`pe-`/`ms-`/`me-`/`start-`/`end-`/`rounded-ss`/`rounded-se`/`justify-start`/`justify-end`) so the whole chat mirrors correctly in RTL. The `Bot` avatar sits on the start side in both directions.
- Lint: `cd /home/z/my-project && bun run lint` → CLEAN (0 errors, 0 warnings). `bunx tsc --noEmit` reports zero errors originating from `AiCoachScreen.tsx` (fixed one initial tsc error: `KEYWORDS` is `as const` so its arrays are `readonly` — changed `hasKeyword`'s param to `readonly string[]`). Remaining tsc errors are pre-existing in `examples/`, `skills/`, and `src/app/page.tsx` (`JSX` namespace) — out of scope.

Stage Summary:
- `AiCoachScreen` is a NAMED export at `src/components/screens/AiCoachScreen.tsx`, already wired into `src/app/page.tsx`'s `SCREENS` lookup for `route.name === "ai-coach"`. Reach it via `navigate("ai-coach")` (the Header's "AI Tools" dropdown already does this). The coach can in turn route the user to `navigate("course-landing", { courseId })` (from a whatNext recommendation) or `navigate("ai-planner")` (from planner/hours responses).
- Honors all spec requirements: SPA `'use client'` screen; centered max-w-3xl chat; header (title + subtitle + disclaimer + clear); scrollable message list seeded with a localized greeting; coach/user bubbles with the Bot-avatar + violet-circle treatment; typing indicator using the existing `.typing-dot` CSS; 6 suggestion chips that auto-send; Textarea + round Send button with Enter-to-send / Shift+Enter-newline; 900–1300ms simulated thinking via `setTimeout` in the handler; a contextual MOCK response generator with 9 keyword categories (motivation, whatNext, hours, stuck, career, planner, hello, thanks, default) each bilingual (EN + Saudi-AR) and referencing the user's name/role/enrolled-courses/real catalog titles; actionable course + planner buttons; auto-scroll via ref + effect (DOM mutation only); full Arabic + RTL + Saudi dialect support; full accessibility (role=log, role=article, aria-labels, aria-live). No localStorage, no real backend, no real LLM. Lint passes.

---
Task ID: 2-b
Agent: sub-agent (AiPlannerScreen)
Task: Build the AI Learning Planner screen — week-by-week plan generator with MOCK (simulated) AI

Work Log:
- Read the worklog + foundation files first: `src/lib/store.ts` (selectors `user`, `onboardingAnswers` `{roleId,skillIds,...}`, `navigate(name, params)`, `pushToast({title,description?,variant})`, `fireConfetti()`), `src/lib/types.ts` (`RouteName` includes `"ai-planner"`, `"course-landing"`, `"home"`; `Course` shape with `title`/`titleAr`/`category`/`categoryAr`/`level`/`totalHours`/`totalLessons`/`skills`/`skillsAr`/`tags`/`accent`; `Role` shape with `id`/`label`/`labelAr`/`skillIds`/`recommendedCourseIds`/`icon`), `src/hooks/use-i18n.ts` (`useI18n()` → `{t, locale, isRTL, formatNumber, formatPrice, formatDate, formatDuration}`), `src/lib/mock-data.ts` (`courses`, `roles`, `getCourse`, `getRole`, `skills`, `getSkill`), `src/lib/i18n.ts` (all `aiPlanner.*` keys present in en/arSa/arEg: title, subtitle, navDesc, goal, goalPlaceholder, role, weeklyHours, timeline, level + `.beginner/.intermediate/.advanced`, generate, generating, regenerate, savePlan, saved, planTitle `{weeks}/{goal}`, planSummary, totalWeeks/totalHours/totalCourses `{n}`, week `{n}`, focus, hoursThisWeek `{n}`, courses, milestone, milestones, tips, empty, weeksShort `{n}`, hrsWeek `{n}`, viewCourse, addToPlan, confidence, confidenceNote `{n}`; plus reused `card.hours`, `card.lessons`, `nav.home`, `nav.welcome`, `nav.aiPlanner`), `src/app/globals.css` (`.view-enter`, `.typing-dot`, `--primary #a617e8` neon-violet, `--gold #f5c518`, `--success #1e7f45`), and the shadcn primitives `button`, `card`, `input`, `label`, `select`, `slider`, `badge`, `separator`, `skeleton`. Skimmed `OnboardingScreen.tsx` for the role-icon map + `aria-pressed` chip pattern and `CourseLandingScreen.tsx` for the sticky-sidebar 2-col layout pattern.
- Created `/home/z/my-project/src/components/screens/AiPlannerScreen.tsx` — NAMED export `AiPlannerScreen`, `'use client'` at top. Self-contained (module-scope types `Level`/`PlanWeek`/`GeneratedPlan`, the pure `generateMockPlan` function, static lookups `ROLE_ICONS`/`FOCUSES`/`MILESTONES`/`TIPS`/`TIMELINE_OPTIONS`/`SIMILAR_LEARNERS`, and sub-components `PlannerForm`, `PlanDisplay`, `WeekCard`, `CourseMiniCard`, `StatCell`, `PlanSkeleton` all in the same file). No foundation files modified. No new shared components added. The module was already referenced by `src/app/page.tsx` — it now resolves.
- Layout: `max-w-7xl mx-auto` root with a header (sparkles pill + H1 `aiPlanner.title` + subtitle + optional welcome line), then a 2-column `lg:grid lg:grid-cols-[360px_minmax(0,1fr)]` body. Left = the form (`<aside>` with `lg:sticky lg:top-20` so it stays visible while the plan scrolls). Right = the plan (`<section>` showing either the skeleton, the generated plan, or an EmptyState). On mobile the form stacks above the plan.
- Form (`PlannerForm`): shadcn `Card` with `gap-0 py-0` so the header sits flush. Card title = `Target` icon + `aiPlanner.goal`. Body sections separated by `<Separator>`:
  1. Goal `Input` (text, `aria-label`, `disabled={generating}`, placeholder `aiPlanner.goalPlaceholder`).
  2. Role chips (`aiPlanner.role`): `flex flex-wrap` of pill `<button aria-pressed>`s, one per `roles` entry. Each chip carries the role's lucide icon (mapped via `ROLE_ICONS`) + the locale-aware label (`locale === "ar" ? r.labelAr : r.label`). Clicking calls `onPickRole(id)` → the parent sets `roleId` and fills `goal` with `locale === "ar" ? \`أصبح ${r.labelAr}\` : \`Become a ${r.label}\``.
  3. Weekly hours `Slider` (`aiPlanner.weeklyHours`): `min=2 max=20 step=1 value={[weeklyHours]}` + `aria-label`. Live readout in a `Badge` showing `aiPlanner.hrsWeek {n}` (formatNumber). Min/max labels under the slider for clarity.
  4. Timeline `Select` (`aiPlanner.timeline`): options 4/8/12/16/24 weeks (default 12). Each `SelectItem` is labeled `aiPlanner.totalWeeks {n}` so the dropdown reads naturally in both locales.
  5. Level cards (`aiPlanner.level`): 3-col grid of `<button aria-pressed>` toggles for beginner/intermediate/advanced. Selected card uses `border-primary bg-primary/5 text-primary ring-2 ring-primary/15`.
  6. Generate `Button` (`size="lg"`, `w-full`): `disabled={!canGenerate}` where `canGenerate = goal.trim() && !generating`. `aria-busy={generating}`. Label/icon swaps between `Wand2 + aiPlanner.generate` and `RefreshCw (animate-spin) + aiPlanner.generating`.
- Pre-fill from onboarding: `useState` lazy initializers read `onboardingAnswers.roleId`. If set, `roleId` defaults to it and `goal` defaults to `Become a {role label}` (or `أصبح {labelAr}` in Arabic). No `useEffect` needed — initial state is computed once at mount.
- Generate flow (`handleGenerate`): guards on `goal.trim()` → `setGenerating(true)` + `setPlan(null)` (clears any previous plan so the skeleton shows) → tracks the timer in a `useRef` → `window.setTimeout(() => {…}, 1400)` INSIDE the click handler (not in an effect — satisfies `react-hooks/set-state-in-effect`). On fire: builds the new plan via `generateMockPlan`, `setPlan(newPlan)`, `setGenerating(false)`, `fireConfetti()`, and `pushToast({ title: aiPlanner.planSummary, description: aiPlanner.planTitle {weeks, goal}, variant: "success" })`. A cleanup-only `useEffect(() => () => clearTimeout(timerRef.current), [])` clears the timer if the user navigates away mid-build (no `setState` in the effect body — pure cleanup).
- Save flow (`handleSave`): `pushToast({ title: aiPlanner.saved, variant: "success" })`. Regenerate flow reuses `handleGenerate` (re-runs the 1.4s skeleton + new plan + confetti).
- Mock plan generator (`generateMockPlan` — module scope, pure, deterministic-ish):
  1. Seed course pool: if `roleId` is set, lead with `getRole(roleId).recommendedCourseIds` (real catalog lookup). Then augment with keyword matches from the goal text (split on non-letter Unicode, filter len ≥ 3, match against `title + category + skills + tags` lowercased). If still under 4, top up with the most popular courses by `enrolledCount`. Cap pool at 4–8.
  2. Distribute courses across `timelineWeeks`: each course `i` lands at week `floor(i * timelineWeeks / courseCount) + 1` (1-indexed). With courseCount ≤ timelineWeeks this spreads courses 1-per-week; with courseCount > timelineWeeks some weeks get 2.
  3. Build week objects: hours = `weeklyHours ± 1` (deterministic `((w+1)*7) % 3 - 1`, clamped ≥1); focus cycles through `FOCUSES` (Foundations → Core skills → Hands-on practice → Project building → Advanced patterns → Deep dive → Real-world case studies → Review & polish, each with `en`/`ar`); milestone every 3rd week (weeks 3/6/9/…) pulling from `MILESTONES` (6 entries each bilingual), with the final week always landing on a milestone if any are left.
  4. Totals: `totalHours` = sum of weekly hours; `totalCourses` = unique course count; `confidence` = `min(96, 80 + min(courseCount*2, 14) + levelBonus)` (beginner +2, intermediate +1, advanced +0) → lands in 80–96 range.
  5. Tips: 4 bilingual coach tips (Pomodoro, revisit-yesterday, build-alongside-week-3, take-notes-by-hand) carried in both `tips` and `tipsAr`.
- Plan display (`PlanDisplay`): three regions.
  1. Header row: H2 = `aiPlanner.planTitle {weeks, goal}` + muted `aiPlanner.confidenceNote {n: 1240}`. Right-aligned action buttons: outline `Regenerate plan` (`RefreshCw`) and primary `Save plan` (`Save`).
  2. Plan summary `Card`: 3-col grid of `StatCell`s (CalendarClock/totalWeeks, Clock/totalHours, Target/totalCourses) → Separator → confidence indicator (Trophy icon + `aiPlanner.confidence` label + success-green `%` Badge + a custom progress bar that grows from the start edge in both LTR and RTL via `width: {confidence}%` on a `bg-muted` track, with `role="progressbar"` + `aria-valuenow/min/max`) + `aiPlanner.confidenceNote` line.
  3. Week-by-week timeline: a vertical `<ol>` with a 1px rail on the start side (`absolute top-3 bottom-3 w-px bg-border start-2`). Each `<li className="relative ps-7">` carries a dot centered on the rail (`start-2` + `-translate-x-1/2` in LTR / `translate-x-1/2` in RTL via `isRTL` ternary; `ring-4 ring-background` masks the rail behind the dot; `bg-gold + Flag` icon on milestone weeks, `bg-primary` otherwise). Each week is an `<article>` (`WeekCard`) with: header row (primary `Week N` Badge + focus text + secondary hours Badge with Clock), tiny `aiPlanner.focus` label, courses list (`<ul>` of `CourseMiniCard`s), and a milestone row (gold-tinted box with Trophy circle + `aiPlanner.milestone` micro-label + the locale-aware milestone text).
  4. Coach tips `Card`: `Lightbulb` title + `<ul>` of tips, each with a gold lightbulb chip.
  5. Subtle ghost "Home" button (locale-aware arrow) at the bottom.
- `CourseMiniCard`: a real `<button>` wrapping a 96px-wide `CourseThumb` + title (locale-aware, line-clamp-2, group-hover:text-primary) + a success-tinted "In plan" Badge (`Check` + `aiPlanner.addToPlan`) + meta row (locale-aware category · level + `card.hours {n}` + `card.lessons {n}`). Clicking → `navigate("course-landing", { courseId })`. Carries `aria-label = aiPlanner.viewCourse: {title}`.
- Empty state: when no plan yet (and not generating), the right column renders `<EmptyState icon={Wand2} title=aiPlanner.title description=aiPlanner.empty className="bg-card border border-border rounded-xl" />`.
- Loading skeleton (`PlanSkeleton`): a header shimmer + a summary card with 3 skeleton stat cells + 4 skeleton week cards + the existing `.typing-dot` triple-dot animation + `aiPlanner.generating` label.
- React Hooks rule compliance: `useEffect` is used ONLY as a cleanup-only unmount hook (no `setState` in its body) — `() => () => { if (timerRef.current) window.clearTimeout(timerRef.current); }`. The "generating" delay uses `setTimeout` INSIDE the click handler (not in an effect). All `setState` calls happen inside event handlers or the `setTimeout` callback. `generateMockPlan` is module-scope (not a recursive component-scope function) → satisfies `react-hooks/immutability`. Fixed one initial tsc error: `LucideIcon` is exported from `lucide-react`, not `react` (moved from `import { type LucideIcon } from "react"` to a separate `import type { LucideIcon } from "lucide-react"`).
- i18n / RTL: every visible string + aria-label goes through `t()`. Locale-aware course titles via `locale === "ar" ? course.titleAr : course.title` (same for category, role label, focus, milestone, tips). All layout uses logical CSS (`ps-`, `pe-`, `ms-`, `me-`, `start-`, `end-`, `text-start`). The timeline dot translate flips with `isRTL ? "translate-x-1/2" : "-translate-x-1/2"`. The back-to-home arrow flips between `ArrowLeft` (LTR) and `ArrowRight` (RTL). `<html dir>` is synced by the existing `DirSync` component.
- Accessibility: H1 = aiPlanner.title; H2 = aiPlanner.planTitle; week cards are `<article>`s; week list is a semantic `<ol>/<li>`; form controls carry `aria-label`; slider/select/inputs are labelled; role + level chips use `aria-pressed`; generate button carries `aria-busy`; confidence bar carries `role="progressbar"` + `aria-valuenow/min/max`; mini course cards carry `aria-label`.
- Design system: violet primary (`bg-primary`, `text-primary`, `border-primary/40`, `ring-primary/15`), gold milestones (`bg-gold`, `text-gold`, `border-gold/40`), success-green confidence (`bg-success`, `text-success`, `border-success/40`), warm white-smoke neutral background inherited from `bg-background`/`bg-card`, rounded-md/lg cards via shadcn `Card`, mobile-first responsive (`lg:grid` 2-col, `grid-cols-3` for level cards + summary stats, `flex flex-wrap` for chips).
- Lint: `cd /home/z/my-project && bun run lint` → CLEAN (0 errors, 0 warnings). `bunx tsc --noEmit` reports zero errors originating from `AiPlannerScreen.tsx` (the only remaining tsc errors are pre-existing in `examples/`, `skills/`, and `src/app/page.tsx`'s `JSX` namespace — out of scope per the foundation-file protection rules).

Stage Summary:
- `AiPlannerScreen` is a NAMED export at `src/components/screens/AiPlannerScreen.tsx`, already wired into `src/app/page.tsx`'s `SCREENS` lookup for `route.name === "ai-planner"`. Reach it via `navigate("ai-planner")` (the Header's "AI Tools" dropdown already does this). The planner can in turn route the user to `navigate("course-landing", { courseId })` (from a mini course card) or `navigate("home")` (from the subtle back link).
- Honors all spec requirements: SPA `'use client'` screen; sticky 2-column desktop layout (form left, plan right) with mobile stacking; form with goal input, role chips (with auto-fill goal text), weekly hours slider (2–20), timeline select (4/8/12/16/24), level cards (beginner/intermediate/advanced with aria-pressed), and a prominent Generate button; prefill from `onboardingAnswers.roleId`; 1.4s setTimeout INSIDE the click handler to simulate "generating"; pure `generateMockPlan` that seeds courses from the role's recommended list + goal keyword matches, distributes them across the timeline, assigns weekly focus + hours + milestones + tips, and computes a confidence score (80–96); confetti + success toast on completion; plan display with H2 title, 3-stat summary card, confidence indicator (badge + custom RTL-safe progress bar), vertical week-by-week timeline (rail + dots that flip with dir + milestone rows), and a coach-tips card; EmptyState before generation; loading skeleton during generation; full Arabic + RTL + Saudi/Egyptian dialect support via logical CSS and `t()`; full accessibility (article week cards, aria-pressed chips, aria-busy button, role=progressbar, aria-labels throughout). No localStorage, no real backend, no real LLM. Lint passes.

---
Task ID: 7
Agent: main (orchestrator)
Task: Add Instructor Profile, AI Planner, AI Coach + navbar with page links

Work Log:
- Added 3 new route names to `src/lib/types.ts`: `instructor-profile`, `ai-planner`, `ai-coach`.
- Added ~100 new i18n keys across all 3 dictionaries (en/arSa/arEg) in `src/lib/i18n.ts`: `nav.instructors`, `nav.aiPlanner`, `nav.aiCoach`, `nav.more`, `nav.explore`, `instructor.*` (16 keys), `aiPlanner.*` (36 keys), `aiCoach.*` (21 keys). Saudi tone uses "يلا/هلا" style; Egyptian uses "يلا بينا/نوّرت" style.
- Updated `src/components/layout/Header.tsx` to add an "Explore" dropdown (Sparkles icon + chevron) in the primary nav, containing "AI Planner" (CalendarRange icon) and "AI Coach" (Bot icon) with descriptions. Active route highlights. Works on mobile (icon-only) and desktop (icon+label).
- Updated `src/app/page.tsx` to import + register the 3 new screens in the SCREENS lookup: `InstructorProfileScreen`, `AiPlannerScreen`, `AiCoachScreen`.
- Updated `src/components/screens/CourseLandingScreen.tsx` `InstructorSection` to make the instructor avatar + name clickable → `navigate("instructor-profile", { instructorId })`, and added a "View full profile" outline button. Added `instructor.viewProfile` key to all 3 dicts.
- Launched 3 parallel subagents (Task IDs 2-a, 2-b, 2-c) to build the screens. All returned with clean lint.
- Agent Browser verification:
  1. **Nav**: "Explore" dropdown shows AI Planner + AI Coach with descriptions. Active state highlights.
  2. **AI Planner**: Pre-filled goal "Become a Frontend Developer" from onboarding role. Slider (5h/wk), timeline (12 weeks), level cards. "Generate my plan" → 1.4s loading → 12 week-cards with focus, hours, courses (clickable → course-landing), milestones, coach tips, confidence score. Confetti fired on generation.
  3. **AI Coach**: Greeting by name. 6 suggestion chips. Sent "How do I stay motivated?" → contextual motivation response. Sent "What career advice do you have for a frontend developer?" → career response referencing "Frontend Developer" role. Mock keyword-matching works across 9 categories.
  4. **Instructor Profile**: Navigated from course landing "View full profile" → Omar Farouk profile with H1, ABOUT bio, 4 stat cards (Rating 4.5, 221K students, 5 courses, 88 hours), specialties pills, all 5 courses as CourseCards, aggregated student reviews with show-more.
  5. **Arabic/RTL**: Switched to Arabic on AI Coach → `<html dir="rtl" lang="ar">`, title "مدرّب التعلّم بالذكاء", Arabic suggestion chips. Sent "وش أتعلّم بعدين?" → Arabic (Saudi) response with course recommendation "«React 19 من الصفر...»".
- No runtime errors, no console warnings. `bun run lint` clean. Dev server HTTP 200.

Stage Summary:
- 3 new feature screens added: InstructorProfileScreen, AiPlannerScreen, AiCoachScreen.
- Navbar expanded with "Explore" dropdown linking to the 2 AI tools.
- Instructor profile reachable from any course landing's instructor section.
- All AI responses are fully mocked (no real LLM) with 9+ keyword categories, locale-aware (EN + Saudi Arabic + Egyptian Arabic).
- All new strings translated across all 3 dictionaries with proper dialect tone.
- The app now covers: Auth → Onboarding → Home → Search → Course Landing → Instructor Profile → Cart → Checkout → Course Player → Quiz → My Learning → AI Planner → AI Coach.
